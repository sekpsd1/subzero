# Product Specifications Library

- Route: `/trade-resources/product-specifications#numberOfResults=20`
- Menu: Professionals > Product Specification Library points to the route above.
- Page files: `src/app/trade-resources/product-specifications/page.tsx`, `src/components/ProductSpecifications/ProductSpecificationsPage.tsx`, and its CSS module.
- Data: the Sub-Zero grid uses all 98 current reference records from the Coveo result set, including model names, model numbers, prices, product-detail destinations, and product-image URLs. Wolf search/filter fallback continues to use the project's typed `products` array from `src/lib/site-data.ts`. The schema has no product-document, CAD, or legacy-document records.
- Search and filters: model/name search, brand selection, the source-accurate Sub-Zero `Type` and `Style` multi-select facets with verified memberships/counts, single-choice Wolf/Cove product categories, empty results, working 20-item pagination, Next and View All controls, accessible result counts, and shareable hash state. Type/style selections use OR within a facet and are persisted in the URL hash; hash changes restore state for browser Back/Forward.
- Documents: design guides, serial-number help, and legacy destinations use hrefs inspected from the live USA reference. External documents open safely in a new tab. Product-document and local CAD downloads show honest unavailable states because no local API/assets exist.
- Legacy: Wolf, Sub-Zero, and Cove lists and destinations were transcribed from the live reference into structured arrays and rendered by brand. Anchor sections use scroll offsets and reduced-motion-safe behavior.
- Responsive: desktop uses a 274px filter rail and a reference-sized three-column result grid; tablet reduces the grid; mobile uses a category dropdown and single-column results. Browser checks passed at 1920px and 390px with no horizontal overflow.
- Visual refinement: the default `#numberOfResults=20` state now selects Sub-Zero and renders the supplied 20-product first page, reference pager metadata (`VIEW ALL 98`), 260×372px cards, product imagery, prices, single-brand legacy tabs, and the corrected two-column help CTA. At 1920px, the H1, section headings, first result card, legacy heading, and help heading align to the measured USA vertical positions.
- Validation: `npm.cmd run lint` and `npm.cmd run build` passed. Browser interaction checks passed for Page 2, Next, Page 5, View All 98, exact first/last models, verified Designer 22 and All Refrigerator 27 facet totals, search, brand switching, legacy tabs, reload state, and mobile layout. Browser logs contained no page errors; the existing unrelated Turbopack AVIF warning remains.
- Integration limitation: only 22 active local product records exist and Cove has no local product record. No product-specific specification/manual files or CAD bundles are present, so those downloads await a real document integration.

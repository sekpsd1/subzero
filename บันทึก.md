## อัปเดต Designer Series 2026-07-06

- หน้าเป้าหมายตอนนี้คือ `/refrigeration/designer-series` เท่านั้น
- ปรับ section `Integrated door hinge` ให้ใกล้ reference USA ล่าสุด:
  - เพิ่มความกว้าง desktop ของสองคอลัมน์จาก `1128px` เป็น `1410px`
  - ปรับภาพซ้าย/ขวาให้ใหญ่ขึ้นตามเรฟจอกว้าง
  - เอา margin-top ของภาพขวาออก เพื่อให้ด้านบนของสองภาพเริ่มเท่ากันตามภาพที่ผู้ใช้ส่งล่าสุด
  - ลดฟอนต์ caption/body ลงเล็กน้อยให้บางและใกล้ USA
  - เพิ่ม top padding ของ section กลับเป็นประมาณ `130px` บน desktop เพื่อให้มีพื้นที่ว่างด้านบนเหมือน reference USA
- ตรวจแล้ว `npm run lint` ผ่าน
- ตรวจแล้ว `npm run build` ผ่าน

## อัปเดต Designer Series color palette 2026-07-06

- ปรับ section `Designed to fit your vision` ให้ใกล้ reference USA:
  - ยกเลิก max-width เดิมที่ทำให้ content แคบเกินไป
  - ปรับหัวข้อ desktop ให้เป็นบรรทัดเดียวและอยู่ซ้ายแบบ reference
  - ลดขนาดหัวข้อ desktop ลง เพื่อไม่ให้ชนกับ copy ด้านขวา
  - ขยับ copy ไปด้านขวาและลดการบีบความกว้าง
  - ลดระยะก่อนภาพหลักลง เพื่อให้รูปเริ่มสูงใกล้ reference
  - ขยายภาพ palette ให้กว้างเกือบเต็ม viewport
  - เอา overlay สีน้ำเงินโปร่งทับช่วงล่างของภาพออก
  - ปรับพื้นที่ล่างและตำแหน่งถาดสีให้ยังอยู่บนพื้นหลังสีน้ำเงิน ไม่หลุดลงพื้นครีม
  - ปรับ color wheel ตาม reference code ให้เป็นครึ่งวงใหญ่ขึ้น ใช้การวางสีแบบ orbit และหมุนทีละ 45 องศาตาม 8 สี
  - แก้ color wheel ไม่ให้ถูกตัดเหลือเสี้ยว โดยแยกครึ่งวงเป็น layer เหนือขอบรูป และให้ label เริ่มใต้ขอบรูปบนพื้นหลังสีน้ำเงิน
  - ลด mask ครึ่งวงของ color wheel จาก `95px` เป็น `86px` เพื่อให้วงไม่โผล่สูงเกิน reference
  - ปรับ color wheel รอบล่าสุดให้วงเล็กลงเป็น `170px`, จุดศูนย์กลางลงมาต่ำกว่าเดิม, ปุ่ม active ลด scale และ label ขยายเป็น `0.95rem` ให้ใกล้ reference
  - ใช้ GIF `1783349538582.gif` เป็น reference behavior ล่าสุด: color wheel ต้อง mask เห็นเฉพาะครึ่งบนเหนือขอบรูป, active color อยู่บนสุด, ปุ่มสีหมุนตาม active, มี marker dot กลางเส้นขอบ และ label อยู่ใต้เส้นบนพื้นสีน้ำเงิน
  - เพิ่ม `transition-all duration-500` ให้ swatches เพื่อให้ปุ่มสีวิ่งรอบวงตอนเลือกสี ไม่กระโดดแข็ง
  - ปรับ geometry ล่าสุดตามรูปเทียบ: wheel diameter `178px`, mask `89px`, radius swatches `62px`, ขยับ center ซ้าย `10px`, และแยก marker dot ออกมาวางบนเส้นแบ่งรูป/แถบน้ำเงินโดยตรง
  - เปลี่ยนจากสูตร orbit เป็น fixed slot geometry เพื่อให้ swatches อยู่ตำแหน่งเหมือน reference มากขึ้นและไม่เลื่อนไปผิดครึ่งวง
  - หลังผู้ใช้ยืนยันว่า function ใช้ได้แล้วแต่ visual wheel ยังเบี้ยว: ปรับ visual geometry เป็น wheel diameter `206px`, mask `103px`, เอา offset ซ้าย `10px` ออก และตั้ง fixed slots ใหม่ให้วงสมมาตรกว่าเดิมตาม reference
  - ปรับต่อหลัง visual ยังเบี้ยว: ลด wheel กลับเป็น diameter `180px`, mask `90px`, และตั้ง fixed slots ใหม่ให้ lower swatches อยู่บนเส้นแบ่งและถูกตัดครึ่งใกล้ reference
  - ปรับ visual wheel อีกครั้ง: ลดเป็น diameter `176px`, mask `88px`, แก้ fixed slots และตัด Tailwind translate/scale class ที่ซ้อนกับ inline transform เพื่อไม่ให้ dot เบี้ยว
  - ปรับอีกนิดตามภาพเทียบล่าสุด: ลด wheel เป็น `172px`, mask `86px`, ลด active ring/scale และดัน label ขึ้นเล็กน้อย
  - ปรับอีกนิดรอบล่าสุด: เอา ring ชั้นนอกของ active swatch ออก, ลด active scale เป็น `1.03`, และขยับ label ลงด้วย `pt-4`
  - ปรับตามภาพเทียบล่าสุด: ยกชุด wheel/label ขึ้น `8px` และเพิ่ม active swatch scale/ring เล็กน้อยให้ใกล้ reference
  - ปรับล่าสุดตามภาพเทียบ: ขยาย wheel กลับเป็น diameter `176px`, mask `88px`, fixed slots ชุด `176px` และคง active swatch แบบโปร่งใส
  - แก้โครงสร้าง color wheel: เปลี่ยนจาก absolute ที่ยึดกับรูป เป็น control band จริงใต้รูป แล้วให้ wheel ยื่นขึ้นจาก band เพื่อไม่ให้ดูเหมือนลอยและไม่ถูกตัดผิดจาก mask ซ้อน
- ตรวจแล้ว `npm run lint` ผ่าน
- ตรวจแล้ว `npm run build` ผ่าน

# บันทึกโปรเจค Sub-Zero Wolf SEA

อัปเดตล่าสุด: 2026-06-30

ไฟล์นี้ใช้สำหรับเริ่มแชทใหม่กับ Codex เพื่อให้จำบริบทโปรเจคต่อจากแชทเดิมได้

## สถานะโปรเจคโดยรวม

- โปรเจคอยู่ที่ `C:\Codex projects\zub-zero`
- Stack: Next.js 16.2.9, React 19, Tailwind CSS, Prisma
- Repository GitHub: `https://github.com/sekpsd1/subzero`
- Host/Plesk: `new.subzerowolf-sea.com`
- ภาษาหน้าเว็บ: English เท่านั้น
- เว็บอ้างอิงหลักด้านดีไซน์: `https://www.subzero-wolf.com/`
- เว็บอ้างอิงสินค้า: `https://au.subzero-wolf.com/en/`
- Sitemap / โครงสร้างข้อมูลหลัก: อ้างอิงจากไฟล์ Word ที่ผู้ใช้ส่งไว้
- แบรนด์ที่ใช้ตอนนี้: Sub-Zero และ Wolf เท่านั้น
- ห้ามเพิ่ม Cove / Dishwashing ถ้าผู้ใช้ไม่ได้สั่ง

## คำสั่งที่ใช้บ่อย

- Run dev บน Windows PowerShell:
  - ถ้า `npm.ps1` ติด execution policy ให้ใช้ `npm.cmd`
  - `npm.cmd run dev`
- Build:
  - `npm.cmd run build`
- Lint:
  - `npm.cmd run lint`
- Plesk:
  - ใช้ Node.js `20.20.2`
  - Package manager เลือก `npm`
  - Build ต้องรัน `npm install` ก่อน แล้ว `npm run build`
  - Startup file: `app.js`

## สิ่งที่ทำไปแล้ว

1. ตั้งค่า Next.js project และ routing พื้นฐาน
2. สร้างหน้าแรกตามเว็บ USA หลาย section
3. สร้าง/ปรับ Header ให้มี logo ด้านบนและเมนูแบบ overlay
4. สร้าง Mega Menu ให้ใกล้เคียงเว็บ USA
5. ปรับเมนูให้ไม่มี Cove/Dishwashing ตามเว็บปัจจุบัน
6. ทำหน้าแรก section 1 hero video
7. ทำหน้าแรก section 2 product showcase พร้อม hover video เปิด/ปิดประตูสินค้า
8. ทำหน้าแรก section 3 showroom journey
9. ทำหน้าแรก section 4 ice maker feature
10. ทำหน้าแรก section 5 layered videos แบบ scroll-driven stack
11. ทำหน้าแรก section 6 trusted by the best
12. ทำหน้าแรก section 7 designed with you in mind
13. ทำหน้าแรก section 8 experience more
14. ทำ Footer ใกล้เคียงเว็บ USA พร้อม accordion, social links, back-to-top
15. Push/Deploy ผ่าน GitHub + Plesk แล้วบางรอบ
16. เริ่มสร้างหน้า `discover-sub-zero`

## ไฟล์สำคัญ

- หน้าแรก: `src/app/page.tsx`
- Header: `src/components/Header/Header.tsx`
- Mega Menu: `src/components/MegaMenu/MegaMenu.tsx`
- Footer: `src/components/Footer/Footer.tsx`
- Home sections:
  - `src/components/Hero/Hero.tsx`
  - `src/components/Home/TriBrandShowcase.tsx`
  - `src/components/Home/ShowroomJourney.tsx`
  - `src/components/Home/IceMakerFeature.tsx`
  - `src/components/Home/LayeredVideos.tsx`
  - `src/components/Home/TrustedByBest.tsx`
  - `src/components/Home/DesignedWithYou.tsx`
  - `src/components/Home/ExperienceMore.tsx`
- Discover Sub-Zero page:
  - route: `src/app/refrigeration/discover-sub-zero/page.tsx`
  - component: `src/components/ProductPages/DiscoverSubZeroPage.tsx`
- Shared data:
  - `src/lib/site-data.ts`

## Assets สำคัญ

- Home hero:
  - `public/assets/subzero/home-hero.avif`
  - `public/assets/subzero/home-hero.mp4`
- Tri-brand section videos:
  - `public/assets/subzero/wolf-tb.mp4`
  - `public/assets/subzero/sub-zero-tb.mp4`
  - `.webm` variants มีในโฟลเดอร์เดียวกัน
- Section 4:
  - `public/assets/subzero/dice-unveil-cut-wr.avif`
- Section 5 videos:
  - `public/assets/subzero/video-sec5/foresight-innovation.mp4`
  - `public/assets/subzero/video-sec5/foresight-engineering.mp4`
  - `public/assets/subzero/video-sec5/longevity.mp4`
  - `public/assets/subzero/video-sec5/service.mp4`
- Section 6:
  - `public/assets/subzero/testimonial-joel.mp4`
- Logos:
  - `public/assets/subzero/sub-zero-logo.svg`
  - `public/assets/subzero/wolf-logo.svg`
  - `public/assets/subzero/white-logo-sub-zero.png`
  - `public/assets/subzero/white-logo-wolf-2.png`

## ข้อห้ามสำคัญจากผู้ใช้

- ห้ามแก้หน้าแรกแล้ว ถ้าผู้ใช้ไม่ได้สั่ง
- ห้ามแก้ Header ถ้าผู้ใช้ไม่ได้สั่ง
- ห้ามแก้ Footer ถ้าผู้ใช้ไม่ได้สั่ง
- ห้ามแก้ Mega Menu ถ้าผู้ใช้ไม่ได้สั่ง
- ห้ามแก้ Section 2 ของหน้าแรก ถ้าผู้ใช้ไม่ได้สั่ง เพราะ hover เปิด/ปิดประตูเคยพังหลายรอบ
- เวลาผู้ใช้ระบุว่าแก้ section ไหน ให้แก้เฉพาะ section นั้น
- อย่าแก้โค้ดปนหลายส่วน
- ก่อนแก้ดีไซน์ให้เทียบกับรูป reference: รูปแรกที่ผู้ใช้ส่งมักเป็น USA, รูปที่สองมักเป็นเว็บที่เราทำ

## งานล่าสุดที่กำลังทำ

กำลังทำหน้า:

`/refrigeration/discover-sub-zero`

เว็บ reference:

`https://www.subzero-wolf.com/refrigeration/discover-sub-zero`

สิ่งที่ผู้ใช้ต้องการตอนล่าสุด:

- Section 1 hero ต้องเป็น video แบบ USA
- ผู้ใช้ย้ำว่าปัจจุบันที่ทำ “ไม่เหมือน” เพราะเราใช้วิดีโอ/เฟรมที่ไม่ตรงกับ reference
- reference ที่ผู้ใช้ต้องการคือ video hero ที่มี mood แบบหน้า USA discover-sub-zero ไม่ใช่ภาพนิ่ง
- ห้ามไปแตะหน้าแรก, header, footer, menu หรือ section อื่นในระหว่างแก้จุดนี้

สถานะทางเทคนิคตอนล่าสุด:

- `src/components/ProductPages/DiscoverSubZeroPage.tsx` มีตัวแปร:
  - `heroVideo`
  - `heroPoster`
- URL ที่ใช้อยู่ตอนนี้:
  - `https://delivery-p28264-e87620.adobeaemcloud.com/adobe/assets/urn:aaid:aem:16d5b94a-6377-43ab-a3b0-df858ae6ca3d/play?assetname=sz-hero.mp4`
  - poster: `https://delivery-p28264-e87620.adobeaemcloud.com/adobe/assets/urn:aaid:aem:62b796d9-e925-4168-bada-74a03f01ae24/as/sz-hero-backup.avif?assetname=sz-hero-backup.png`
- ผู้ใช้บอกว่ายังไม่ถูก ต้องเช็คกับเว็บ USA อีกครั้งก่อนแก้

## Git status ล่าสุดก่อนบันทึก

ยังมีไฟล์เปลี่ยนแปลง/เพิ่มใหม่:

- modified: `src/app/products/[...slug]/page.tsx`
- modified: `src/lib/site-data.ts`
- untracked: `src/app/refrigeration/`
- untracked: `src/components/ProductPages/`

ยังไม่ได้ commit หลังงาน discover-sub-zero ล่าสุด

## แนวทางทำงานต่อในแชทใหม่

1. อ่านไฟล์นี้ก่อน
2. ยืนยันกับผู้ใช้ก่อนว่า “กำลังทำเฉพาะหน้า discover-sub-zero section 1 hero video”
3. ห้ามแก้ไฟล์ homepage/header/footer/menu
4. เช็ค reference USA อีกครั้งเพื่อหา video source/behavior ที่ถูกต้อง
5. แก้เฉพาะ `src/components/ProductPages/DiscoverSubZeroPage.tsx`
6. รัน lint/build หลังแก้
7. ถ้าถูกแล้วค่อยถามผู้ใช้ว่าจะ commit/push หรือไป section ถัดไป

## อัปเดตล่าสุด 2026-06-30

ผู้ใช้อนุญาตให้แก้ Header แล้วเฉพาะจุดที่สั่งในรอบนี้

งานที่ทำเสร็จล่าสุด:

1. หน้า `/refrigeration/discover-sub-zero`
   - แก้ Section 1 hero ให้เป็นวิดีโอแบบ USA ไม่มีข้อความ/CTA ทับวิดีโอ
   - ใช้วิดีโอ local จาก `C:\Codex projects\zub-zero\img-SUB-ZERO\sz-hero.mp4`
   - คัดลอกไฟล์ไปไว้ที่ `public/assets/subzero/sz-hero.mp4`
   - ใน `src/components/ProductPages/DiscoverSubZeroPage.tsx` ตั้ง `heroVideo` เป็น `/assets/subzero/sz-hero.mp4#t=0.001`
   - ปุ่ม pause/play ของ hero video อยู่ขวาล่างเหมือน reference USA

2. Header
   - แก้เฉพาะ `src/components/Header/Header.tsx`
   - ปรับเงา/gradient ตอนอยู่บน hero ให้เป็น fader โปร่งแบบ USA ไม่ใช่แถบดำทึบ
   - หลัง scroll พ้น Section 1 แล้วเลื่อนขึ้น Header จะเปลี่ยนเป็นพื้นสว่าง `#f4f2ec`
   - ตอน Header เป็นพื้นสว่าง icon/text เป็นสีเข้ม และ logo เปลี่ยนเป็นไฟล์สีเข้ม:
     - `public/assets/subzero/wolf-logo.svg`
     - `public/assets/subzero/sub-zero-logo.svg`
   - ตอนอยู่บน hero ยังเป็น Header โปร่งและใช้ logo สีขาว:
     - `public/assets/subzero/white-logo-wolf-2.png`
     - `public/assets/subzero/white-logo-sub-zero.png`
   - ห้ามเพิ่ม Cove ตามที่ผู้ใช้ย้ำไว้

ผลตรวจล่าสุด:

- `npm.cmd run lint` ผ่าน
- `npm.cmd run build` ผ่าน
- ตรวจด้วย browser local แล้ว:
  - บน hero: Header โปร่ง, logo สีขาว, ไม่มี Cove
  - หลัง scroll พ้น hero แล้วเลื่อนขึ้น: Header พื้นสว่าง, icon/text/logo สีเข้ม, ไม่มี Cove

ข้อควรระวังต่อไป:

- หน้าแรก, Footer, Mega Menu, Section 2 ของหน้าแรก ยังถือว่าล็อกอยู่ ถ้าผู้ใช้ไม่ได้สั่ง
- Header ตอนนี้ผู้ใช้อนุญาตให้แก้เฉพาะเรื่องเงา/สถานะ scroll ตาม reference USA แล้วเสร็จ
- ถ้ามีงานต่อ ให้แก้เฉพาะจุดที่ผู้ใช้ระบุ และรัน `npm.cmd run lint` กับ `npm.cmd run build` หลังแก้

## อัปเดต Plesk 500 / `.htaccess` 2026-06-30

หลัง deploy แล้วเว็บจริง `new.subzerowolf-sea.com` เคยขึ้น `Internal Server Error` บน route เช่น:

- `/refrigeration/discover-sub-zero`
- `/products`
- `/showroom`
- `/journal`

ตรวจ log ใน Plesk พบ error:

`AH00124: Request exceeded the limit of 10 internal redirects due to probable configuration error`

สาเหตุคือ `.htaccess` เดิมทำ Apache internal redirect loop ไม่ใช่โค้ด Next.js พัง

วิธีแก้ที่ทำแล้ว:

- เปลี่ยนชื่อ `.htaccess` บน server เป็น `.htaccess.bak`
- Restart App ใน Plesk
- ตรวจเว็บจริงแล้ว route หลักกลับมา `200`
- เพิ่มไฟล์ `.htaccess` แบบปลอดภัยใน repo โดยมีเฉพาะ comment และไม่มี rewrite rule
- เพิ่ม `.htaccess.bak` ใน `.gitignore`
- เพิ่ม Plesk deploy notes ใน `README.md`

ข้อควรจำ:

- ห้ามเอา `.htaccess.bak` กลับมาเป็น `.htaccess` ถ้ายังไม่ได้ตรวจ rule
- Plesk Node.js / Passenger ใช้ `app.js` เป็น startup file และ Next.js handle route เอง
- หลัง pull/deploy ให้รัน `npm install`, `npm run build`, แล้วกด `Restart App`

## โฟกัสงานปัจจุบัน

ตอนนี้กำลังทำหน้า:

`/refrigeration/discover-sub-zero`

กติกาจากผู้ใช้:

- ทำหน้า `discover-sub-zero` ไปทีละ section จนครบ
- ห้ามเริ่มทำหน้าอื่นเอง
- ถ้าผู้ใช้ต้องการเริ่มหน้าอื่น ผู้ใช้จะบอกชัด ๆ ว่า “เริ่มทำหน้าอื่นนะ”
- ระหว่างนี้ถ้าผู้ใช้ไม่ได้สั่ง ให้ถือว่า scope งานยังอยู่ที่หน้า `discover-sub-zero`

## อัปเดตล่าสุด 2026-07-06 12:56 +07

ตอนนี้เปลี่ยนมาทำหน้า:

`/refrigeration/classic-series`

Reference หลัก:

`https://www.subzero-wolf.com/refrigeration/classic-series`

กติกาปัจจุบันจากผู้ใช้:

- กำลังทำหน้า `classic-series` ทีละ section
- ถ้าผู้ใช้บอกว่า “จบเซคชั่น” ให้หยุดแก้ section นั้น และห้ามกลับไปแตะเองจนกว่าผู้ใช้จะสั่ง
- หน้า `discover-sub-zero` ก่อนหน้านี้ถือว่าจบงานรอบนั้นแล้ว อย่าไปแก้เอง
- ตอนนี้ผู้ใช้บอกแล้วว่า Section 1 ของหน้า `classic-series` จบ

งานที่ทำแล้วในหน้า `classic-series`:

1. สร้าง route จริง:
   - `/refrigeration/classic-series`
   - ไม่ใช้ `/products/refrigeration/classic-series` เป็นหน้าหลัก

2. Section 1 hero:
   - ทำเป็นวิดีโอ local จากโฟลเดอร์ `refrigeration-classic-series`
   - มีปุ่ม pause/play มุมขวาล่าง
   - ปรับภาพ/ตำแหน่งให้ใกล้ reference USA แล้ว
   - ผู้ใช้บอก “จบเซคชั่นที่ 1” แล้ว ห้ามแก้ section นี้เอง

3. Section 2 intro:
   - ปรับหัวข้อ `Classic Series` ให้เล็กลงจากของเดิมที่ใหญ่เกิน reference
   - ปรับ layout/spacing ให้ใกล้ USA
   - พื้นหลังเป็น stone อ่อน `#f4f2ec`
   - เส้นแนวตั้งและ snowflake เป็นสีน้ำเงิน Sub-Zero

4. บล็อกรูป grille ต่อจาก intro:
   - เอาข้อความใหญ่ที่ทับรูปออก
   - ปรับเป็นรูปเดี่ยวกลางหน้าแบบ reference
   - ใช้กรอบ 16:9 และจำกัดความกว้างให้ใกล้ต้นฉบับ

ไฟล์หลักที่เกี่ยวข้องกับหน้านี้:

- `src/app/refrigeration/classic-series/page.tsx`
- `src/components/ProductPages/ClassicSeriesPage.tsx`
- `public/assets/subzero/refrigeration-classic-series/`
- route/menu ที่เคยแตะไว้:
  - `src/lib/site-data.ts`
  - `src/components/Footer/Footer.tsx`
  - `src/app/products/[...slug]/page.tsx`

ผลตรวจล่าสุด:

- `npm.cmd run lint` ผ่าน
- `npm.cmd run build` ผ่าน

สถานะ git ล่าสุดที่ควรจำ:

- มีไฟล์ใหม่/ไฟล์แก้จากงาน `classic-series` ยังไม่ได้ commit หลังรอบล่าสุด ถ้าผู้ใช้สั่ง `commit/push` ค่อยทำ
- ห้าม revert งานเก่าหรือไฟล์ที่ไม่ได้เกี่ยวกับคำสั่งปัจจุบัน

งานต่อไป:

- ถ้าผู้ใช้กลับมาทำต่อ ให้ถาม/ดูจากคำสั่งล่าสุดว่าเริ่ม Section 3 หรือจะแก้ Section 2 เพิ่ม
- เริ่มจากอ่าน reference USA และแก้เฉพาะ section ที่ผู้ใช้สั่ง
- หลังแก้ทุกครั้งให้รัน:
  - `npm.cmd run lint`
  - `npm.cmd run build`

## อัปเดตล่าสุด 2026-07-06 - กติกาการทำงานใหม่จากผู้ใช้

ผู้ใช้ย้ำว่าจากนี้จะใช้ “1 แชท = 1 หน้า” เพื่อไม่ให้บริบทปนกันและทำงานหลุดหน้าเดิม

หัวใจหลักที่ต้องจำ:

- ทุกแชทต้องอ่าน/ยึด `บันทึก.md` ก่อนเริ่มงาน ถ้ามีบริบทขัดกันให้ยึดคำสั่งล่าสุดของผู้ใช้
- โปรเจกต์นี้ทำเว็บให้ใกล้ reference USA `https://www.subzero-wolf.com/` มากที่สุด
- เวลาผู้ใช้แนบรูปเปรียบเทียบ:
  - รูปที่ 1 คือ reference USA ที่ต้องทำตาม
  - รูปที่ 2 คือเว็บ local ที่ต้องแก้
- ห้ามตัดสินว่า “เหมือนแล้ว/ผ่านแล้ว” จากตัวเลข DOM หรือขนาด element อย่างเดียว ต้องดูภาพรวมด้วยตาเทียบ reference
- ปัญหาซ้ำที่ต้องระวังมาก: ฟอนต์มักใหญ่เกิน reference USA ต้องเช็กและลดให้ใกล้ต้นฉบับ
- แก้เฉพาะ section/page ที่ผู้ใช้สั่ง ห้ามไปแตะหน้าแรก, Header, Footer, Mega Menu หรือ section อื่นเองถ้าไม่ได้สั่ง
- ถ้าผู้ใช้บอกว่า “ไม่เหมือนแบบ” ให้ถือว่างานยังไม่ผ่าน หยุดสรุปว่าเสร็จ แล้วกลับไปเทียบ reference ใหม่
- ควรบันทึกความคืบหน้า, งานที่ทำ, งานที่ยังไม่ผ่าน, และข้อควรระวังลงไฟล์นี้เป็นระยะ

สถานะล่าสุดของหน้า `/refrigeration/classic-series`:

- ผู้ใช้ไม่พอใจกับรอบแก้ carousel “More Classic Series features” ล่าสุด และบอกว่ายังไม่เหมือนแบบ
- ห้ามถือว่า carousel section นี้ผ่านแล้ว
- รอบต่อไปถ้าจะกลับมาแก้ section นี้ ต้องเทียบภาพ reference ใหม่ก่อน และแก้ด้วยสายตา/spacing จริง ไม่ใช่วัดเฉพาะขนาด DOM
- ผู้ใช้ต้องการ workflow ที่รวดเร็วแต่แม่นกว่าเดิม: ดู reference, แก้เฉพาะจุด, ตรวจภาพ, แล้วค่อยสรุป

## อัปเดตล่าสุด 2026-07-06 - แก้ carousel Classic Series features

หน้า: `/refrigeration/classic-series`

Section: `More Classic Series features`

งานที่แก้รอบนี้:

- ย้าย controls `1 / 4` และปุ่มลูกศรออกจาก sticky บน viewport ให้กลับมาอยู่แถวเหนือรูปแบบ reference USA
- ปรับหัวข้อให้ลอยสูงขึ้นจนใกล้ภาพ reference ที่หัวข้อโดน crop ด้านบน
- คงตำแหน่งรูปหลักและ preview ด้านขวาให้ใกล้ reference
- ย้ายปุ่ม `Visit a showroom` มาอยู่แถว caption ใต้รูป ไม่ให้หลุดลงไปด้านล่าง
- ดัน progress bar ลง ไม่ให้โผล่ใน viewport แรกของ section ตามภาพ reference

ข้อควรจำ:

- ถ้าผู้ใช้ยังบอกว่าไม่เหมือน ให้เทียบ screenshot ใหม่ก่อนแก้ต่อ
- Header fixed อาจโผล่เวลาใช้ `window.scrollTo` ใน browser test เพราะไม่ได้จำลอง scroll direction จริง; ตอนเทียบกับภาพผู้ใช้ให้ดูเฉพาะ section และถ้าต้องตรวจ header ให้ scroll ด้วย gesture จริง

## อัปเดตล่าสุด 2026-07-06 - Sticky CTA Classic Series

หน้า: `/refrigeration/classic-series`

งานที่แก้:

- เพิ่ม sticky CTA ปุ่ม `Visit a showroom` เป็น fixed pill ด้านล่างกลางจอ
- ปุ่มนี้ซ่อนตอนอยู่ Section 1 hero video
- ปุ่มเริ่มแสดงเมื่อ scroll มาถึง Section 2 intro ที่มีข้อความ `Sub-Zero`, `Classic Series`, และคำโปรย `For those seeking...`
- ใส่ marker `data-classic-intro` ที่ intro section เพื่อควบคุมจุดเริ่มแสดงปุ่ม
- รัน `cmd /c npm run lint` ผ่านแล้ว

## อัปเดตล่าสุด 2026-07-06 - ใช้ section DesignedWithYou จากหน้าแรก

หน้า: `/refrigeration/classic-series`

งานที่แก้:

- นำ component `DesignedWithYou` จาก `src/components/Home/DesignedWithYou.tsx` มาใช้ในหน้า `classic-series`
- แทนที่ section gallery เดิมของหน้านี้ด้วยฟังก์ชันแบบหน้าแรก: กดแท็บ Transitional / Traditional / Contemporary, กดลูกศรเปลี่ยนรูป, มีรูปหลักและรูป preview ด้านขวา
- รอบถัดมาผู้ใช้ชี้ชัดว่าให้เอา section นี้ไปแทน carousel `More Classic Series features` เดิมที่เป็น `4 / 4`
- ตอนนี้ `DesignedWithYou` ถูกย้ายไปแทนตำแหน่ง `FeatureCarousel` เดิมแล้ว และลบ `FeatureCarousel` / `classicFeatures` เดิมออกจาก `ClassicSeriesPage.tsx`
- ลบ function `DesignGallery` เดิมและ `galleries` เดิมออกจาก `ClassicSeriesPage.tsx`
- รัน `cmd /c npm run lint` ผ่านแล้ว

อัปเดตเพิ่มเติม:

- เปลี่ยนรูปใน section `DesignedWithYou` ของหน้า `classic-series` ให้ใช้ไฟล์ local จาก `public/assets/subzero/refrigeration-classic-series`
- เพิ่มไฟล์รูป:
  - `classic-series-gallery-1.avif`
  - `classic-series-gallery-2.avif`
  - `classic-series-gallery-3.avif`
  - `classic-series-gallery-4.avif`
  - `classic-series-gallery-5.avif`
- หน้าแรกยังใช้รูปเดิม เพราะ `DesignedWithYou` ถูกปรับให้รับ props ได้
- หน้า `classic-series` ส่ง `categories={classicSeriesDesignCategories}` เข้าไปเอง
- ลบไฟล์ frame `.png` ชุดที่ capture ผิดและไม่ได้ใช้ออกแล้ว
- รัน `cmd /c npm run lint` ผ่านแล้ว

อัปเดต caption:

- section `DesignedWithYou` รองรับ caption แบบ optional ต่อ slide แล้ว
- หน้า `classic-series` ใช้ 4 รูป / 4 caption:
  - `1 / 4` Panel ready — Seamlessly accepts custom panels to integrate perfectly with cabinetry.
  - `2 / 4` Flip-up dairy compartment — Enables storage of taller bottles on the shelf below.
  - `3 / 4` Nano-coated glass — Spill-proof shelves treated with a special finish make cleanup simple.
  - `4 / 4` Maximum flexibility — Adjustable shelves and door bins easily adapt to fit items of varying shapes and sizes.
- หน้าแรกยังไม่กระทบ เพราะไม่ได้ส่ง caption props เข้าไป

อัปเดตเพิ่มเติม:

- ผู้ใช้สั่งลบแท็บ `Transitional / Traditional / Contemporary` ออกจาก section นี้บนหน้า `classic-series`
- เพิ่ม prop `showTabs` ให้ `DesignedWithYou`
- หน้า `classic-series` ส่ง `showTabs={false}` เฉพาะจุดนี้
- หน้าแรกยังแสดงแท็บตามเดิม

## อัปเดตล่าสุด 2026-07-06 - Section aesthetic fusion

หน้า: `/refrigeration/classic-series`

งานที่แก้:

- เพิ่ม section `The perfect fusion of elegance and performance, Classic Series enhances any kitchen aesthetic.` กลับเข้ามา
- วางหลัง `Start your journey in a showroom` และก่อน `Personalize to find your perfect fit`
- ใช้ asset เดิม:
  - `assets.galleryTraditional`
  - `assets.galleryTransitional`
  - `assets.galleryContemporary`
- มี tab Traditional / Transitional / Contemporary, counter, รูปใหญ่ และปุ่ม `See more inspiration`
- รัน `cmd /c npm run lint` ผ่านแล้ว

## Update 2026-07-06 - Fix aesthetic fusion layout to USA reference

Page: `/refrigeration/classic-series` only

Changed:

- Reworked `DesignGallery` from a single-image layout to the USA-style grid.
- Layout now follows the requested reference: small left-aligned heading, tabs below, top row with wide image + narrow image, second row full-width image, bottom centered `See more inspiration` button.
- Removed the section counter from this gallery.
- Background is `#f4f2ec`, matching the section above.
- Each tab now owns 3 images instead of 1 image.
- Verified in local browser that the section has 3 images and the second row spans the full gallery width.
- Ran `cmd /c npm run lint` successfully.

## Update 2026-07-06 - Personalize model section

Page: `/refrigeration/classic-series` only

Changed:

- Reworked `Personalize to find your perfect fit` from a one-model interactive detail layout into a USA-style model listing.
- Section now has left-aligned heading/copy, `See all Classic Series Refrigeration` CTA on the right, and 5 model cards in one desktop row.
- Model cards shown: Column, French Door, Over-and-Under, Side-by-Side, Wine Storage.
- Each card includes product silo image, model title, description, and `Explore this model` button.
- Reduced heading sizing from the previous oversized treatment.
- Verified in local browser that the section renders 5 cards across desktop.
- Ran `cmd /c npm run lint` successfully.
- Ran `cmd /c npm run build` successfully.

## Update 2026-07-07 - PRO Series design carousel second alignment pass

Page: `/refrigeration/pro-series` only

User feedback:

- Carousel still did not match the USA reference.

Changed:

- Reduced section top padding so the heading and carousel move up.
- Reduced heading size again and narrowed heading max width to match the reference line breaks more closely.
- Reduced spacing between heading, carousel controls, and media.
- Kept the larger/taller carousel images from the previous pass.

Browser verification at a `1905px`-wide viewport:

- Section starts at `y=0` in the verification viewport.
- Heading measured about `900 x 83`, positioned around `x=249`, `y=48`.
- Heading font size measured `39.2px`.
- Main image measured about `1274 x 725`, positioned around `x=249`, `y=212`.
- Preview image measured about `363 x 725`, positioned around `x=1535`, `y=212`.
- Counter remained `1 / 5`.
- No horizontal overflow detected.

Verification commands:

- Ran `cmd /c npm run lint` successfully.
- Ran `cmd /c npm run build` successfully.

## Update 2026-07-07 - PRO Series design carousel visual sizing pass

Page: `/refrigeration/pro-series` only

Reference from user screenshots:

- The PRO design carousel should visually match the USA reference more closely.
- Heading should be smaller and wider so it wraps into two lines, not three.
- Carousel media should start higher and appear larger/taller.
- Main image should be wider, with a narrower preview image at the right.

Changed:

- Reduced the carousel heading size from `48px` desktop to about `42.4px`.
- Increased heading max width to `1040px`.
- Reduced spacing between heading, controls, and media.
- Increased carousel media height to `725px` on large desktop.
- Reduced preview column width from `26%` to `22%`, giving more width to the main image.

Browser verification at a `1905px`-wide viewport:

- Heading measured about `1040 x 90`, wrapping into two lines.
- Heading font size measured `42.4px`.
- Main image measured about `1274 x 725`.
- Preview image measured about `363 x 725`.
- Counter remained `1 / 5`.
- No horizontal overflow detected.

Verification commands:

- Ran `cmd /c npm run lint` successfully.
- Ran `cmd /c npm run build` successfully.

## Update 2026-07-07 - PRO Series design carousel copied from Classic features pattern

Page: `/refrigeration/pro-series` only

User request:

- Replace the current PRO `Discover how the PRO Series...` section with the same behavior as the `More Classic Series features` carousel from `/refrigeration/classic-series`.
- Keep the PRO USA reference content and imagery.

Changed:

- Reworked the PRO `DesignGallery` section from tabbed `Traditional / Transitional / Contemporary` grid into a carousel based on the Classic `DesignedWithYou` interaction pattern.
- Removed the old PRO gallery tab data.
- Added a PRO-specific five-image carousel data set from the existing PRO AEM assets.
- Added `1 / 5` counter and previous/next controls.
- Kept dark PRO theme to match the USA reference.
- Main image plus right-side preview image now match the Classic carousel functionality.

Browser verification at a `1905px`-wide viewport:

- Heading rendered: `Discover how the PRO Series' striking design fits seamlessly across different aesthetics and styles`.
- Counter rendered as `1 / 5`.
- Previous and next buttons are present.
- Main image measured about `1208 x 668`.
- Preview image measured about `429 x 668`.
- Clicking next updated the counter to `2 / 5`.
- No horizontal overflow detected.

Verification commands:

- Ran `cmd /c npm run lint` successfully.
- Ran `cmd /c npm run build` successfully.

## Update 2026-07-07 - PRO Series hotspot image full reference framing

Page: `/refrigeration/pro-series` only

Reference from user screenshots:

- Hotspot image section should show the full original composition like the USA reference.
- The first hotspot should appear as a dark open card with title and minus control, not as a white closed pill.
- The open hotspot card should not display extra image/copy that blocks the appliance image.

Changed:

- Reworked the hotspot image frame from a narrow/tall `aspect-[1.64]` container to viewport-based height:
  - desktop: `height: calc(100svh - 124px)`
  - `min-height: 760px`
  - `max-height: 860px`
- Kept the hotspot image full-width with `object-cover object-center`.
- Added a shared desktop hotspot data array and made `Stainless steel or glass door` open by default.
- Restyled the open hotspot to match the USA dark card:
  - width about `326px`
  - min-height `180px`
  - dark background
  - white title
  - minus circle on the right
- Hid the desktop detail copy/image inside the open card so it does not cover the product image.

Browser verification at a `1905 x 995` viewport:

- Hotspot section frame measured `1890 x 860`.
- Open hotspot card measured `326 x 180`.
- Open hotspot background: `rgb(31, 30, 28)`.
- Open hotspot text color: white.
- No horizontal overflow detected.

Verification commands:

- Ran `cmd /c npm run lint` successfully.
- Ran `cmd /c npm run build` successfully.

## Update 2026-07-07 - PRO Series Lighting feature size correction

Page: `/refrigeration/pro-series` only

Reference from user screenshots:

- `Lighting` row should not be the small local version.
- Image should be large on the left, with heading/copy/link on the right.
- Visual size should match the USA feature pattern more closely.

Changed:

- Removed conflicting responsive grid classes that could make the `Lighting` image stay at the smaller column width.
- Kept `Air purification` as text-left/image-right.
- Set the `Lighting` image to the same large aspect ratio as the `Stainless steel interior` hero feature.

Browser verification at a `1905px`-wide viewport:

- `Lighting` image measured `820 x 710`.
- `Lighting` heading/copy rendered on the right column.
- `Explore products` link is present.
- No horizontal overflow detected.

Verification commands:

- Ran `cmd /c npm run lint` successfully.
- Ran `cmd /c npm run build` successfully.

## Update 2026-07-07 - PRO Series feature card layout match

Page: `/refrigeration/pro-series` only

Reference from user screenshots:

- `Stainless steel interior` section should use a large left image with copy on the right.
- The first image should be much larger than the previous local version.
- `Explore products` link should appear under the feature copy.
- `Air purification` should begin lower, with text on the left and a large image on the right.

Changed:

- Reworked the `FeatureCards` layout from a compact repeated two-column grid into a USA-style feature layout.
- Increased the feature container to `1440px`.
- Set the first image to a larger `820px`-wide visual at wide desktop.
- Added `Explore products` under feature copy.
- Corrected the second row so `Air purification` text sits on the left and the large image sits on the right.
- Kept the change scoped to `src/components/ProductPages/ProSeriesPage.tsx`.

Browser verification at a `1905px`-wide viewport:

- `Stainless steel interior` first image measured `820 x 710`, starting around `x=225`.
- `Stainless steel interior` heading/copy rendered to the right of the image.
- `Air purification` image measured `820 x 603`, starting around `x=745`, `y=856`.
- `Air purification` heading/copy rendered on the left.
- `Explore products` links are present.
- No horizontal overflow detected.

Verification commands:

- Ran `cmd /c npm run lint` successfully.
- Ran `cmd /c npm run build` successfully.

## Update 2026-07-07 - PRO Series sticky showroom CTA exact match

Page: `/refrigeration/pro-series` only

Reference from user DevTools:

- Button size: `144.02 x 37`
- Text color: `#FBF9F5`
- Background: `#18181799`
- Font: `14px "Museo Sans", museo-sans`
- Padding: `10px 16px`

Changed:

- Locked the fixed `Visit a showroom` CTA to `width: 144.02px` and `height: 37px`.
- Locked padding, line-height, font stack, and font weight inline so Tailwind utility classes do not shift the final computed shape.
- Removed the previous extra shadow and `min-height` treatment because it made the local pill look heavier than the USA reference.

Browser verification:

- Computed size: `144.02 x 37`
- Background: `rgba(24, 24, 23, 0.6)`
- Text color: `rgb(251, 249, 245)`
- Font stack: `"Museo Sans", museo-sans, Arial, Helvetica, sans-serif`
- Font size/weight: `14px / 700`
- Padding: `10px 16px 10px 16px`
- Box shadow: `none`
- Page scroll width matched viewport width; no horizontal overflow found in this check.

Verification commands:

- Ran `cmd /c npm run lint` successfully.
- Ran `cmd /c npm run build` successfully.

## Update 2026-07-07 - PRO Series sticky showroom button exact DevTools values

Page: `/refrigeration/pro-series` only

Reference:

- User provided USA DevTools values for `.floating-cta-btn.button.primary`:
  - color `#FBF9F5`
  - background `#18181799`
  - font `14px`
  - padding `10px 16px`

Changed:

- Updated the sticky `Visit a showroom` CTA to use the exact USA DevTools values:
  - `backgroundColor: "#18181799"`
  - `color: "#FBF9F5"`
  - `fontSize: "14px"`
  - `lineHeight: "17px"`
  - `padding: "10px 16px"`

Verification:

- Browser computed style check confirmed:
  - background is `rgba(24, 24, 23, 0.6)`
  - color is `rgb(251, 249, 245)`
  - font size is `14px`
  - padding is `10px 16px 10px 16px`
- Ran `cmd /c npm run lint` successfully.
- Ran `cmd /c npm run build` successfully.

## Update 2026-07-07 - PRO Series sticky showroom button forced color

Page: `/refrigeration/pro-series` only

Changed:

- Darkened the sticky `Visit a showroom` CTA again after user confirmed the color still looked unchanged.
- Removed the Tailwind background utility from the sticky CTA and added an inline `backgroundColor: "#514f49"` so the exact color is forced even if another style or cached utility output conflicts.
- Kept the compact `144px x 38px` target sizing from the previous pass.

Verification:

- Ran `cmd /c npm run lint` successfully.
- Ran `cmd /c npm run build` successfully.

## Update 2026-07-07 - PRO Series sticky showroom button correction

Page: `/refrigeration/pro-series` only

Reference:

- User provided a close comparison of the sticky `Visit a showroom` button.
- USA reference button is a compact dark gray pill; local button was too light/warm.

Changed:

- Adjusted only the PRO Series sticky `Visit a showroom` CTA.
- Changed the pill background from `#74716a` to a darker neutral gray `#5f5d57`.
- Reduced horizontal padding from `20px` to `18px`.
- Reduced the shadow slightly so the button feels closer to the USA reference.
- Updated hover color to `#514f4a`.

Verification:

- Browser measurement confirmed the sticky CTA renders about `144px x 38px`.
- Browser measurement confirmed computed background is `rgb(95, 93, 87)`.
- Browser measurement confirmed document scroll width equals client width.
- Ran `cmd /c npm run lint` successfully.
- Ran `cmd /c npm run build` successfully.

## Update 2026-07-07 - PRO Series freshness title scale correction

Page: `/refrigeration/pro-series` only

Reference:

- User provided a comparison where the USA `The art of freshness` section has a much larger serif headline and larger cauliflower bowl image, with the next feature section not appearing immediately in the same viewport.

Changed:

- Adjusted only the `The art of freshness` section.
- Increased the section desktop height with `md:min-h-[900px]` so the next feature row no longer appears too early.
- Expanded the section max width from `1120px` to `1420px`.
- Increased the headline scale from a max of about `5.75rem` to a larger responsive clamp up to `8.25rem`.
- Increased the cauliflower image max size from `500px` to `720px`.
- Updated image `sizes` to match the larger desktop asset display.

Verification:

- Browser check on `http://127.0.0.1:3000/refrigeration/pro-series` confirmed:
  - freshness section height is about `900px`
  - heading renders about `97px` at the checked desktop viewport
  - cauliflower image renders about `558px` at the checked desktop viewport
  - next feature section starts below the current viewport
  - document scroll width equals client width
- Ran `cmd /c npm run lint` successfully.
- Ran `cmd /c npm run build` successfully.

## Update 2026-07-07 - PRO Series parallax media reference correction

Page: `/refrigeration/pro-series` only

Reference:

- User provided a comparison where the USA reference left media panel shows a close interior refrigerator-food frame, while the local page showed a wider/full-appliance video frame.

Changed:

- Adjusted the parallax media pair only.
- Changed both media panels from `aspect-[0.88]` to taller `aspect-[0.8]` to match the USA reference proportions.
- Changed the parallax video start fragment from `#t=0.001` to `#t=8` so the left panel opens on the interior-food frame instead of the grille/display/full-appliance frames.
- Added a `frameStyle` option to `VideoFrame` so absolute fill positioning no longer conflicts with the component's default relative positioning.
- Added a `videoStyle` option to `VideoFrame` so this specific parallax video can reliably use `object-fit: cover`, `object-position`, and slight scale without relying on Tailwind arbitrary selector output.
- Fixed the video sizing so the left panel fills the full portrait frame and no longer leaves black space under the video.

Verification:

- Browser check confirmed:
  - left and right parallax panels are equal size at desktop viewport: about `577x721`
  - parallax video frame fills the full panel
  - parallax video starts around `#t=8` and shows the interior-food frame
  - document scroll width equals client width
- Ran `cmd /c npm run lint` successfully.
- Ran `cmd /c npm run build` successfully.

## Update 2026-07-07 - PRO Series full reference dark-flow pass

Page: `/refrigeration/pro-series` only

Reference:

- User provided a full-page USA screenshot for `https://www.subzero-wolf.com/refrigeration/pro-series`.
- Key visual target from the screenshot: after the hero, the intro, parallax media, `The art of freshness`, feature rows, hotspot, design gallery, showroom CTA, model gallery, and expanding image all stay on a dark background before returning to the cream comparison section.

Changed:

- Kept edits scoped to the PRO Series page component.
- Converted the post-hotspot design gallery from cream to dark to match the USA page flow.
- Converted the showroom CTA wrapper background from cream to dark while keeping the inner showroom card light.
- Converted the model gallery from cream to dark with white text, white-outline buttons, and dark segmented controls.
- Converted the expanding kitchen image section frame/background from cream to dark so the page stays dark until the comparison table.
- Reduced later-section heading sizes, especially `Discover how...` and `For kitchens that mean business`, to avoid the oversized-font issue.

Verification:

- Browser check on `http://127.0.0.1:3000/refrigeration/pro-series` confirmed:
  - intro, freshness, features, hotspot, design gallery, showroom wrapper, model gallery, and expanding image backgrounds are `rgb(23, 23, 21)`
  - comparison section returns to cream `rgb(244, 242, 236)`
  - document scroll width equals client width
  - video controls are visible as `58x58` absolute buttons inside their video frames
  - later heading sizes are about `29px` for `Discover how...` and `45px` for `For kitchens that mean business` at desktop viewport
- Ran `cmd /c npm run lint` successfully.
- Ran `cmd /c npm run build` successfully.

## Update 2026-07-07 - PRO Series closer USA reference pass

Page: `/refrigeration/pro-series` only

Reference:

- `https://www.subzero-wolf.com/refrigeration/pro-series`
- The USA page structure to follow is: hero video, intro, parallax media, `The art of freshness`, feature blocks, hotspot image, design gallery, showroom CTA, model gallery, expanding kitchen media, comparison table, and `More to discover`.

Changed:

- Kept the canonical PRO URL as `/refrigeration/pro-series`.
- Confirmed `/products/refrigeration/pro-series` redirects to `/refrigeration/pro-series`.
- Added the showroom CTA section after the PRO design gallery to match the USA `Start your journey in a showroom` placement.
- Updated PRO model/gallery/filter links from `PRO+Series+Refrigeration` to `PRO+Series` to match the USA reference.
- Updated the comparison table order to `PRO Series`, `Designer Series`, `Classic Series`.
- Added the USA PRO comparison image asset id `08979add-b21f-4476-a316-0353f8487da0`.
- Fixed a small horizontal overflow from full-bleed media by removing the extra `100vw` overflow and adding page-level overflow protection.
- Checked typography scale:
  - desktop H1 is about `60px`
  - mobile H1 at `390px` viewport is about `44.8px`
  - mobile body has no horizontal overflow

Verification:

- Browser desktop check:
  - URL is `http://127.0.0.1:3000/refrigeration/pro-series`
  - `main > section` count is `12`
  - comparison order is `PRO Series`, `Designer Series`, `Classic Series`
  - document scroll width equals client width
- Browser mobile check at `390x844`:
  - document scroll width equals client width
  - showroom CTA section exists
- `curl -I http://127.0.0.1:3000/products/refrigeration/pro-series` returns `307` with location `/refrigeration/pro-series`.
- Ran `cmd /c npm run lint` successfully.
- Ran `cmd /c npm run build` successfully.

## Update 2026-07-07 - PRO Series video control refinement

Page: `/refrigeration/pro-series` only

Reference:

- User provided a small screenshot of the USA video pause control: translucent dark-gray center, thin white circular progress ring, and narrow pause bars.

Changed:

- Refined the PRO Series `VideoButton` SVG only.
- Reduced the control size from `60px` to `52px`.
- Changed the center from a heavy black filled circle to a translucent dark-gray circle.
- Reduced the ring stroke thickness and matched the white progress ring closer to the reference.
- Narrowed and shortened the pause bars.
- Follow-up fix after user screenshot: the control was not visible enough on the bright hero image, so the actual button hit area was increased to `58px`, given `bg-black/20`, shadow, hover contrast, and `z-[90]` so it is clearly visible above the hero video.
- Follow-up hardening after user still did not see the button: added `data-video-control="pro-series"` and inline style forcing `display: grid`, `visibility: visible`, `opacity: 1`, `pointerEvents: auto`, and `zIndex: 120`.
- Follow-up after runtime check found the hero fixed button was still being hidden by visibility state: removed the auto-hide visibility logic for the hero control and pinned the hero control as `position: fixed` with `zIndex: 300`.

Verification:

- Ran `cmd /c npm run lint` successfully.
- Browser check confirmed the hero video button is visible in the viewport, clickable, `58x58`, and positioned at the lower-right of the hero.
- Browser DOM check confirmed the running page has the hero control visible in viewport with `58x58`, `opacity: 1`, `visibility: visible`, `display: grid`, and `zIndex: 120`.
- Final browser DOM check confirmed the running page has the hero control as `position: fixed`, `58x58`, `opacity: 1`, `visibility: visible`, `display: grid`, and `zIndex: 300`.

## Update 2026-07-07 - PRO Series hero video start frame

Page: `/refrigeration/pro-series` only

Reference:

- User provided a comparison where the USA reference hero opens on the exterior Sub-Zero grille/logo frame, while the local page was showing an interior refrigerator shelf frame.

Changed:

- Added a `startAt` option to the shared `VideoFrame` component.
- Applied `startAt={0.001}` to the PRO Series hero video so the hero resets to the beginning on page load.
- The reset runs on `loadedMetadata` and `canplay`, which helps avoid browser state restoring the video at a later timestamp.

Verification:

- Ran `cmd /c npm run lint` successfully.
- Browser reload check confirmed the hero video starts around `0.06s` after load, is playing, uses the USA `pro-series-fallback` poster, and the pause button is visible at `58x58` in the lower-right of the hero.

## Update 2026-07-07 - PRO Series intro/parallax section match

Page: `/refrigeration/pro-series` only

Reference:

- User provided a comparison for the section immediately after the hero.
- USA reference has a black/dark background, white copy, a white `Experience in a showroom` pill, blue vertical line/snowflake, and the following media pair on the same dark background.

Changed:

- Changed the PRO intro block from cream/light theme to dark `#171715`.
- Changed intro copy to white and the showroom CTA to a white pill with dark text.
- Reduced the bottom padding after the snowflake so the media starts closer to the reference.
- Changed the following parallax/media section background to dark `#171715`.
- Widened the media grid max width to `1416px`, reduced the gap, and adjusted image/video aspect ratios and vertical offset to match the reference proportions more closely.
- Removed the hero video control's viewport-pinned behavior so the pause button no longer floats into this intro section.

Verification:

- Ran `cmd /c npm run lint` successfully.
- Browser check confirmed:
  - intro background is `rgb(23, 23, 21)`
  - intro text is white
  - CTA background is `rgb(251, 250, 246)` with dark text
  - parallax section background is `rgb(23, 23, 21)`
  - document width equals client width, so no horizontal overflow
- Ran `cmd /c npm run build` successfully.

## Update 2026-07-07 - PRO Series parallax media crop

Page: `/refrigeration/pro-series` only

Reference:

- User provided a closer comparison of the media pair below the intro.
- USA reference uses two tall portrait-like media panels; the left video is cropped into the refrigerator interior instead of showing the full appliance.

Changed:

- Changed the two parallax media panels from a wide `aspect-[1.05]` layout to a taller `aspect-[0.88]` layout.
- Removed the downward offset on the right image so both panels start on the same row like the reference.
- Kept the dark background and narrow gap between media panels.
- Kept the video control inside the left video panel instead of floating into the intro section.

Verification:

- Ran `cmd /c npm run lint` successfully.
- Browser measurement at desktop viewport confirmed both media panels render at `577x655` with ratio `0.88`.
- Browser check confirmed no horizontal overflow.
- Ran `cmd /c npm run build` successfully.

## Update 2026-07-07 - PRO Series USA reference alignment

Page: `/refrigeration/pro-series` only

Changed:

- Rechecked against the attached USA HTML for `https://www.subzero-wolf.com/refrigeration/pro-series`.
- Fixed key AEM media URLs to match USA markup:
  - hero video now uses `renditions/original/as/Subzero - Pro - Hero_FINAL.mp4`
  - parallax video now uses `renditions/original/as/Sub_Zero_Landing_Quality.mp4`
  - expanding image now uses `SZWC_PA_0519_4021FC_A_GraCol2006`
- Removed the full-page `The showroom experience` section that was not part of the USA page flow.
- Added the USA-style parallax media section after the intro: video column plus hinge image column.
- Reworked `The art of freshness` into the USA-style title-and-image block with the cauliflower image.
- Reworked the three preservation feature cards into full-width alternating media/copy rows closer to the USA fading-media-copy block.
- Reworked the hotspot section to use `pro-hotspot-1` as the main full-width background image with two overlay detail cards at the USA hotspot positions:
  - `73% / 25%` Stainless steel or glass door
  - `22% / 62%` Pull-out shelf
- Added the USA drawer detail image for the pull-out shelf hotspot.
- Browser verification:
  - local URL `/refrigeration/pro-series` loads with title `PRO Series Refrigeration | Sub-Zero | Sub-Zero Wolf SEA`
  - hero/parallax video URLs match USA `renditions/original` paths
  - desktop body scroll width does not exceed viewport
  - mobile viewport `390x844` scroll width stayed below viewport width
  - mobile headings fit without text overflow
- Ran `cmd /c npm run lint` successfully.
- Ran `cmd /c npm run build` successfully.

## Update 2026-07-07 - Designer Series completion

Page: `/refrigeration/designer-series`

Final status:

- Completed the Designer Series page pass against the Sub-Zero USA reference.
- Confirmed the page now uses `/refrigeration/designer-series` instead of `/products/refrigeration/designer-series`.
- Kept the legacy `/products/refrigeration/designer-series` route redirecting to the new URL.
- Confirmed the main content/data sequence matches the reference:
  - hero video
  - intro
  - hotspot video feature section
  - integrated door hinge
  - color palette wheel
  - More Designer Series features
  - showroom CTA
  - design gallery
  - model gallery
  - expanding image
  - comparison cards
  - More to discover
- Final browser data check showed the headings, key copy, images count, and video sources match the USA reference/local rendered page.

## Update 2026-07-07 - Designer Series color wheel center slot

Page: `/refrigeration/designer-series` only

Changed:

- Added the center arched slot/cutout visual inside the color palette wheel so the selector matches the USA reference more closely.
- Kept the existing color wheel interaction unchanged.
- Ran `cmd /c npm run lint` successfully.

## Update 2026-07-07 - Designer Series features carousel

Page: `/refrigeration/designer-series` only

Changed:

- Replaced the custom Designer Series features carousel with the same `DesignedWithYou` component pattern used by `/refrigeration/classic-series`.
- Kept the heading as `More Designer Series features`.
- Used the 4 Designer Series feature images and captions from the USA markup:
  - Touch control panel
  - Nano-coated shelves
  - Flexible storage
  - Stainless accents
- Verified locally at `http://127.0.0.1:3000/refrigeration/designer-series`:
  - section renders with `1 / 4`
  - 2 carousel arrow buttons are present
  - large image and preview image render
  - clicking next changes the section to `2 / 4` and `Nano-coated shelves`
- Ran `cmd /c npm run lint` successfully.

## Update 2026-07-07 - Designer Series showroom CTA match

Page: `/refrigeration/designer-series` only

Changed:

- Adjusted the `Start your journey in a showroom` CTA card to match the USA reference more closely.
- Increased card width to `1120px` and image size to about `426px`.
- Updated copy to the USA reference text:
  - `Our showrooms are where inspiration turns into informed decisions...`
- Changed button label from `Visit a showroom` to `Get started`.
- Verified locally at `http://127.0.0.1:3000/refrigeration/designer-series`:
  - card renders about `1120px` wide
  - image renders about `426x428`
  - button text is `Get started`
- Ran `cmd /c npm run lint` successfully.

## Update 2026-07-07 - Designer Series showroom heading wrap

Page: `/refrigeration/designer-series` only

Changed:

- Adjusted only the `Start your journey in a showroom` heading width in the showroom CTA.
- Removed the narrow text max-width so the heading wraps into 2 lines like the USA reference instead of 3 lines.
- Added an explicit line break after `Start your journey` so this heading is forced to exactly 2 lines.
- Reduced the showroom CTA heading size slightly to better match the reference scale.
- Browser verification:
  - heading width is about `542px`
  - heading renders at about `2` lines
- Ran `cmd /c npm run lint` successfully.

## Update 2026-07-07 - Designer Series aesthetic gallery

Page: `/refrigeration/designer-series` only

Changed:

- Replaced the Designer Series `DesignGallery` section with the same interactive structure used by Classic Series.
- Updated heading to:
  - `With a host of features hiding in plain sight, Designer Series integrates beautifully into any aesthetic.`
- Added Designer Series gallery tabs and image sets from the USA reference HTML:
  - Traditional
  - Transitional
  - Contemporary
- Kept the section scoped to `/refrigeration/designer-series` only.
- Browser verification at `http://127.0.0.1:3000/refrigeration/designer-series`:
  - heading renders
  - 3 tabs render
  - 3 images render for the active tab
  - clicking `Transitional` changes the active tab and swaps the image set
- Ran `cmd /c npm run lint` successfully.

## Update 2026-07-07 - Designer Series model gallery

Page: `/refrigeration/designer-series` only

Changed:

- Reworked `Personalize to find the perfect fit` in Designer Series using the Classic Series model carousel structure.
- Kept Designer-specific headline, copy, CTA, model names, images, and links.
- Added side-peeking model carousel behavior:
  - active model centered
  - adjacent models visible at the left/right edges
  - model copy fades and updates when selecting a model
- Added `data-designer-model-gallery` to make this section easier to verify and maintain.
- Browser verification at `http://127.0.0.1:3000/refrigeration/designer-series`:
  - section renders the Designer heading and CTA
  - 4 model images render
  - model tabs render
  - clicking `Column` changes the active tab and model copy to Column
- Ran `cmd /c npm run lint` successfully.

## Update 2026-07-07 - Designer Series remaining page sections

Page: `/refrigeration/designer-series` only

Changed:

- Continued from the `classic-series` pattern through the end of the Designer Series page.
- Reworked the media break after the model gallery to use the Classic Series sticky zoom / expanding media structure.
- Reworked `Explore more Sub-Zero` to match the Classic Series comparison-card pattern:
  - 3 series cards
  - `View models`
  - `Learn more` for non-active cards
  - `Currently Viewing` for Designer Series
  - Finish / Width / Models / Personalization rows
- Reworked `More to discover` to match the Classic Series two-card pattern with larger max width, image ratio, text scale, and CTA buttons.
- Kept all changes scoped to `/refrigeration/designer-series`.
- Browser verification at `http://127.0.0.1:3000/refrigeration/designer-series`:
  - expanding media exists with sticky wrapper and 1 image
  - `Explore more Sub-Zero` renders 3 cards, 3 `View models`, 2 `Learn more`, and all comparison rows
  - `More to discover` renders 2 cards, 2 images, and both CTA links
- Ran `cmd /c npm run lint` successfully.

## Update 2026-07-06 - Personalize header spacing

Page: `/refrigeration/classic-series` only

Changed:

- Matched the `Personalize to find your perfect fit` header area closer to the USA reference screenshot.
- Increased desktop heading size to 64px and kept a two-line title.
- Increased top/header block height so the CTA sits in the same open horizontal band as the reference.
- Adjusted copy width/line-height and CTA size to match the screenshot more closely.
- Verified in browser metrics: heading 64px, CTA height 52px, background `#f4f2ec`.
- Ran `cmd /c npm run lint` successfully.
- Ran `cmd /c npm run build` successfully.

## Update 2026-07-06 - Personalize tabbed product gallery

Page: `/refrigeration/classic-series` only

Changed:

- Replaced the 5-card model grid with a tabbed product gallery to match the attached USA sample code and screenshot.
- Active model image is centered, with previous/next model previews partially visible at the left and right edges.
- Model description sits to the right of the active product image.
- Bottom controls are pill tabs: Column, French Door, Over-and-Under, Side-by-Side, Wine Storage.
- Removed visible progress line/count from this gallery because the provided screenshot shows only tabs.
- Tabs switch the active product, description, and CTA.
- Verified browser metrics: active image centered in viewport, side preview images positioned off-canvas/edge-visible, description to the right.
- Ran `cmd /c npm run lint` successfully.
- Ran `cmd /c npm run build` successfully.

## Update 2026-07-06 - Fullscreen kitchen image section

Page: `/refrigeration/classic-series` only

Changed:

- Updated the kitchen image section after `Personalize to find your perfect fit`.
- Removed constrained `max-width`, side padding, and vertical padding from this image block.
- Image is now full-bleed with `w-screen`.
- Image height is now full viewport using `h-[100svh]` with `min-h-[640px]`.
- Removed hover scale so the section stays stable and fills the screen cleanly.
- Ran `cmd /c npm run lint` successfully.
- Ran `cmd /c npm run build` successfully.

## Update 2026-07-06 - Expanding media sample markup

Page: `/refrigeration/classic-series` only

Changed:

- Updated the full-screen kitchen image block to mirror the provided USA sample markup.
- Added `expanding-media-wrapper`, `expanding-media`, and `tension-zoom tension-zoom--active` structure.
- Added `data-block-name="expanding-media"` and `data-block-status="loaded"`.
- Added `data-aem-asset-id="e8892ec9-24c4-42de-afde-dcbe37051e76"` on the media wrapper.
- Added `tension-zoom-border` top/right/bottom/left overlay elements and `trigger-line`.
- Updated image alt text to match the provided sample more closely.
- Kept the image full viewport: `w-screen`, `h-[100svh]`, `sizes="100vw"`.
- Ran `cmd /c npm run lint` successfully.
- Ran `cmd /c npm run build` successfully.

## Update 2026-07-06 - Expanding media scroll effect

Page: `/refrigeration/classic-series` only

Changed:

- Added real scroll-driven tension zoom behavior to the expanding media section.
- Section is now taller than one viewport (`h-[155svh]`) so there is scroll distance for the effect.
- Media stays sticky at the top while the effect runs.
- Image starts zoomed in at `scale(1.12)` and eases down to `scale(1)` as the section scrolls.
- The four `tension-zoom-border` overlays start at `8vw` and shrink to `0`, revealing the full image.
- Effect updates on scroll and resize using the section position.
- Ran `cmd /c npm run lint` successfully.
- Ran `cmd /c npm run build` successfully.

## Update 2026-07-06 - Product gallery description fade

Page: `/refrigeration/classic-series` only

Changed:

- Added fade transition for the model description panel in `Personalize to find your perfect fit`.
- Product image/tab changes immediately, while the description text fades out first.
- After 180ms the title/copy/CTA swaps to the new model.
- New description fades upward into place using opacity + translate-y transition.
- Added timeout cleanup to avoid stale animation timers.
- Ran `cmd /c npm run lint` successfully.
- Ran `cmd /c npm run build` successfully.

## Update 2026-07-06 - Expanding media thinner border

Page: `/refrigeration/classic-series` only

Changed:

- Reduced expanding media starting border from `8vw` to `2.1vw`.
- Reduced starting zoom from `scale(1.12)` to `scale(1.035)`.
- Effect now shows only a slim frame at the start, closer to the user's screenshot, instead of a large thick border.
- Ran `cmd /c npm run lint` successfully.
- Ran `cmd /c npm run build` successfully.

## Update 2026-07-06 - Designer Series hero video

Page: `/refrigeration/designer-series` only

Changed:

- Added a dedicated route at `src/app/refrigeration/designer-series/page.tsx`.
- Added a dedicated page component at `src/components/ProductPages/DesignerSeriesPage.tsx`.
- Section 1 is now a full-screen video hero matching the USA Designer Series reference.
- Used the USA hero video asset:
  `Subzero-Designer-Hero-1280-Optimized.mp4`
  from AEM asset id `32319839-f5e9-4ae2-80c3-0dc6a6995223`.
- Used the USA hero poster fallback:
  `designer-series-fallback.avif`
  from AEM asset id `d49584d7-2899-4c44-9323-537229debcb2`.
- Added play/pause control with circular progress in the lower-right corner.
- Added the next content block with `Sub-Zero`, `Designer Series`, USA copy, and `Experience in a showroom` CTA.
- Checked the provided USA HTML: the title/copy content is in the section after the hero video, not overlaid on the video.
- Verified in browser at `http://127.0.0.1:3000/refrigeration/designer-series`:
  video exists, autoplay/muted/loop/playsInline are enabled, hero height fills the viewport, H1 desktop size is about `65.6px`.
- Ran `cmd /c npm run lint` successfully.
- Ran `cmd /c npm run build` successfully.

## Update 2026-07-06 - Explore more and final discovery sections

Page: `/refrigeration/classic-series` only

Changed:

- Completed `Explore more Sub-Zero` comparison data.
- Added `Personalization` row to Classic, Designer, and PRO cards.
- Added separate `View models`, `Learn more`, and `Currently Viewing` button/text behavior.
- Rendered Finish values as separate rows with finish dots.
- Rendered Width and Models values as separate underlined links instead of comma-separated text.
- Updated final `More to discover` section to match the provided reference more closely.
- Final section now uses two wide cards with image on top and split title/copy/button area below.
- Updated second final card to `Why Sub-Zero, Wolf, and Cove?`, copy `Delve into why designers trust...`, CTA `Learn about us`, href `/our-story`.
- Ran `cmd /c npm run lint` successfully.
- Ran `cmd /c npm run build` successfully.

## Update 2026-07-06 - Fix comparison row overlap

Page: `/refrigeration/classic-series` only

Changed:

- Fixed text overlap in `Explore more Sub-Zero` comparison rows.
- The `PERSONALIZATION` label was too wide for the old 120px label column and overlapped the value text.
- Increased desktop label column to 160px.
- Reduced label letter spacing from `0.34em` to `0.26em`.
- Added responsive row behavior: rows stack cleanly on narrower widths instead of overlapping.
- Ran `cmd /c npm run lint` successfully.
- Ran `cmd /c npm run build` successfully.

## Update 2026-07-06 - Final section background

Page: `/refrigeration/classic-series` only

Changed:

- Updated `More to discover` section background from `#f4f2ec` to `#ebe8df` to better match the USA reference screenshot.
- Kept the inner cards light so the card content remains separated from the section background.
- Ran `cmd /c npm run lint` successfully.
- Ran `cmd /c npm run build` successfully.

## Update 2026-07-06 - More to discover scale

Page: `/refrigeration/classic-series` only

Changed:

- Adjusted final `More to discover` section to better match the USA reference scale.
- Reduced section heading from max `5rem` to max `4rem` so it no longer appears oversized/cropped.
- Reduced card title size from max `3.5rem` to max `3rem`.
- Reduced card text panel height/padding so the cards are closer to the reference proportions.
- Changed right card image from close-up Wolf range to showroom people image (`assets.showroom`) to better match the USA reference.
- Ran `cmd /c npm run lint` successfully.
- Ran `cmd /c npm run build` successfully.

## Update 2026-07-06 - Classic Series page complete

Page: `/refrigeration/classic-series`

Status:

- User confirmed this page is complete.
- Final verification before commit:
  - `cmd /c npm run lint` passed.
  - `cmd /c npm run build` passed.
- Commit scope should include only Classic Series page work and supporting route/navigation/shared component changes required by this page.
- Do not include unfinished `/refrigeration/designer-series` files in the Classic Series commit.

## Current focus 2026-07-06 - Designer Series page

Current page scope:

- `/refrigeration/designer-series`
- Reference: `https://www.subzero-wolf.com/refrigeration/designer-series`
- Do not edit other pages unless the user explicitly asks.

Latest completed work:

- Created the dedicated Designer Series route and component.
- Section 1 is a full-screen video hero using the USA AEM hero video.
- The USA HTML confirms the title/copy content is a separate content block after the hero video.
- Local verification passed at `http://127.0.0.1:3000/refrigeration/designer-series`.
- `cmd /c npm run lint` passed.
- `cmd /c npm run build` passed.

## Update 2026-07-06 - Designer Series full page pass

Page: `/refrigeration/designer-series` only

Reference:

- Screenshot: `screencapture-subzero-wolf-refrigeration-designer-series-2026-07-06-19_41_14.png`
- USA HTML attachment for asset ids and section order.

Changed:

- Expanded `src/components/ProductPages/DesignerSeriesPage.tsx` from hero/intro only into a full Designer Series page pass.
- Added sections in the same order as the reference screenshot:
  - hero video
  - intro content block
  - full hotspot refrigerator image with feature labels
  - integrated door hinge image pair
  - blue `Designed to fit your vision` band
  - `More Designer Series features` carousel
  - showroom CTA card
  - inspiration/design gallery
  - `Personalize to find the perfect fit` model gallery
  - full-width kitchen media break
  - `Explore more Sub-Zero` comparison cards
  - `More to discover` cards
- Used USA AEM assets for major imagery, including:
  - `designer-hotspot`
  - `sz-designer-hinge-01_new`
  - `sz-designer-hinge-02_new`
  - `sz-designer-colors-1`
  - Designer features carousel images
  - Designer model silo images
  - `sz-designer-media-break`
  - comparison and more-to-discover images.
- Kept type scale restrained to avoid the recurring oversized-font issue:
  - desktop `Designer Series` H1 verified around `60px`
  - major section headings around `49-51px`
  - card titles around `23-26px`
- Verified local page in browser:
  - page has 12 main sections
  - page has 17 images
  - hero video still uses the USA AEM video source
  - comparison cards and more-to-discover cards render without text overlap
- Ran `cmd /c npm run lint` successfully.
- Ran `cmd /c npm run build` successfully.

Current next step:

- Wait for the user's screenshot comparison of local page vs USA reference and fine-tune spacing/cropping section by section.

## Update 2026-07-06 - Designer Series canonical URL

Page: `/refrigeration/designer-series` only

Changed:

- Updated main navigation Designer Series link from `/products/refrigeration/designer-series` to `/refrigeration/designer-series`.
- Updated footer Designer Series link from `/products/refrigeration/designer-series` to `/refrigeration/designer-series`.
- Added redirect in `src/app/products/[...slug]/page.tsx` so the old URL `/products/refrigeration/designer-series` redirects to `/refrigeration/designer-series`.
- Verified `rg "products/refrigeration/designer-series" src` returns no remaining matches.
- Verified in browser that opening `http://127.0.0.1:3000/products/refrigeration/designer-series` ends at `http://127.0.0.1:3000/refrigeration/designer-series`.
- Ran `cmd /c npm run lint` successfully.
- Ran `cmd /c npm run build` successfully.

## Update 2026-07-06 - Designer Series intro marker

Page: `/refrigeration/designer-series` only

Changed:

- Matched the intro block closer to the USA reference screenshot.
- Replaced the small blue dot under the vertical line with the Sub-Zero snowflake icon.
- Increased the blue vertical line height and spacing so the marker sits lower like the reference.
- Updated the intro copy to use `It's panel ready...` like the USA text while keeping JSX lint-safe output.
- Ran `cmd /c npm run lint` successfully.
- Ran `cmd /c npm run build` successfully.

## Update 2026-07-06 - Designer Series intro copy weight

Page: `/refrigeration/designer-series` only

Changed:

- Adjusted only the intro paragraph under `Designer Series`.
- Reduced the paragraph weight with `font-light` so the text looks thinner like the USA reference.
- Slightly reduced the paragraph font size from the previous desktop scale to avoid the recurring oversized/bold look.
- Ran `cmd /c npm run lint` successfully.
- Ran `cmd /c npm run build` successfully.

## Update 2026-07-06 - Designer Series hotspot image full view

Page: `/refrigeration/designer-series` only

Changed:

- Adjusted only the large hotspot refrigerator image section after the intro.
- Replaced the fixed viewport-height `object-cover` crop with an image-ratio container and `object-contain`.
- The hotspot image now uses the source ratio more closely so the full refrigerator/kitchen scene is visible instead of being cut off.
- Repositioned the three feature labels to sit on the full-view image.
- Browser verification: image natural size is `1440x1350`, rendered as `1120x1037`, with `object-fit: contain`.
- Ran `cmd /c npm run lint` successfully.
- Ran `cmd /c npm run build` successfully.

## Update 2026-07-06 - Designer Series hotspot match USA markup

Page: `/refrigeration/designer-series` only

Changed:

- Reworked the hotspot refrigerator section again using the attached USA markup and screenshot.
- Section is now full-width instead of a narrow centered image block.
- Uses `object-cover` with `object-position: 50% 18%` to match the USA hero-like crop.
- Hotspot card coordinates now follow the USA markup:
  - Clearsight LED lighting: `68% / 18%`
  - Split Climate intelligent cooling system: `30% / 40%`
  - Stainless accents: `58% / 72%`
- Updated card typography to serif, larger, and multi-line like the USA screenshot.
- Added the overlaid `Visit a showroom` pill at the bottom center of this image section.
- Browser verification at local viewport: section spans full viewport width, rendered image is `1265x900`, and all 3 hotspot labels plus CTA are present.
- Ran `cmd /c npm run lint` successfully.
- Ran `cmd /c npm run build` successfully.

## Update 2026-07-06 - Designer Series hotspot no crop correction

Page: `/refrigeration/designer-series` only

Changed:

- Corrected the hotspot image after user feedback that the previous USA-style crop cut the top and bottom.
- Removed the fixed height, `max-h`, `overflow-hidden`, and `object-cover` crop from the hotspot image block.
- The image wrapper now uses the actual image ratio `16/15` and `object-contain`.
- Browser verification:
  - natural image ratio: `1440 / 1350 = 1.0667`
  - rendered image ratio: `1265 / 1186 = 1.0667`
  - `object-fit: contain`
- Result: the hotspot refrigerator image is full-width and not cropped at the head/tail.
- Ran `cmd /c npm run lint` successfully.
- Ran `cmd /c npm run build` successfully.

## Update 2026-07-06 - Designer Series sticky showroom CTA

Page: `/refrigeration/designer-series` only

Changed:

- Added a fixed sticky `Visit a showroom` CTA for the Designer Series page.
- Added `data-designer-intro` to the intro section with `Sub-Zero`, `Designer Series`, and the intro copy.
- Sticky CTA stays hidden during section 1 hero video.
- Sticky CTA begins showing once the user scrolls to the intro section.
- Reused the Classic Series sticky CTA behavior and styling so page behavior stays consistent.
- Browser verification:
  - at hero/top: opacity `0`, pointer events `none`
  - after intro is reached: opacity `1`, pointer events `auto`
- Ran `cmd /c npm run lint` successfully.
- Ran `cmd /c npm run build` successfully.

## Update 2026-07-06 - Designer Series remove hotspot CTA duplicate

Page: `/refrigeration/designer-series` only

Changed:

- Removed the overlaid `Visit a showroom` button from the hotspot refrigerator image section.
- Kept the sticky fixed `Visit a showroom` CTA behavior from the previous update.
- Ran `cmd /c npm run lint` successfully.
- Ran `cmd /c npm run build` successfully.

## Update 2026-07-06 - Designer Series hotspot hover details

Page: `/refrigeration/designer-series` only

Changed:

- Added hover/focus detail drawers to the 3 hotspot cards in the refrigerator image section.
- Used the detail copy and video URLs from the attached USA markup:
  - Clearsight LED lighting
  - Split Climate intelligent cooling system
  - Stainless accents
- On hover/focus, the plus indicator changes to a minus, detail copy appears, and a video media area is revealed.
- Browser verification:
  - closed drawer: opacity `0`, height `0`
  - focused/open drawer: opacity `1`, height about `206px`, video element present
- Ran `cmd /c npm run lint` successfully.
- Ran `cmd /c npm run build` successfully.

## Update 2026-07-06 - Designer Series hotspot video controls

Page: `/refrigeration/designer-series` only

Changed:

- Updated hotspot detail media to behave as real videos, matching the reference screenshots.
- Added `autoPlay`, `muted`, `loop`, and `playsInline` to each hotspot video.
- Added an overlay circular pause/play button in the lower-right of each video.
- The pause/play button toggles the associated video playback.
- Browser verification on the first card:
  - video source is the USA AEM `220037_ClearSight.mp4`
  - `autoPlay`, `muted`, and `loop` are true
  - overlay button is present with `aria-label="Pause"` and `aria-pressed="true"`
- Ran `cmd /c npm run lint` successfully.
- Ran `cmd /c npm run build` successfully.

## Update 2026-07-06 - Designer Series integrated hinge section

Page: `/refrigeration/designer-series` only

Changed:

- Updated the next section after hotspot: `Integrated door hinge`.
- Matched the reference layout more closely:
  - two equal-width image columns
  - left image starts near the top
  - right close-up image starts lower
  - small bold heading and compact copy sit under the left image
- Browser verification:
  - left image renders `560x713`
  - right image renders `560x713`
  - right image starts `96px` lower than left image
  - heading is about `13px`, matching the small reference treatment
- Ran `cmd /c npm run lint` successfully.
- Ran `cmd /c npm run build` successfully.

## Update 2026-07-06 - Designer Series color palette interaction

Page: `/refrigeration/designer-series` only

Changed:

- Reworked the `Designed to fit your vision` section from a static image into an interactive color palette.
- Added the 8 USA color options and AEM images from the attached USA HTML:
  - Blue Bungalow
  - Brown Grain
  - Black Midnight
  - White Stone
  - Midnight Modern
  - Desert Oasis
  - Driftwood Oak
  - Green
- Clicking a color changes:
  - active image
  - section background color
  - selected color name
  - `aria-pressed` state
- Added a rotating wheel/tray style selector at the bottom of the image.
- Hid the sticky fixed showroom CTA while the color palette section is visible so it does not block the color wheel.
- Browser verification:
  - color buttons count: `8`
  - initial active: `Blue Bungalow`, background `rgb(39, 70, 96)`
  - after selecting `Brown Grain`: active image changes to the Nashville AEM asset, background `rgb(190, 162, 126)`, label changes to `Brown Grain`
  - sticky CTA opacity is `0` and pointer events `none` while palette section is in view
- Ran `cmd /c npm run lint` successfully.
- Ran `cmd /c npm run build` successfully.

## Update 2026-07-07 - PRO Series first full page pass

Page: `/refrigeration/pro-series` only

Changed:

- Added a dedicated `src/components/ProductPages/ProSeriesPage.tsx` component.
- Added the clean route `/refrigeration/pro-series` to render the new PRO page with `Header` and `Footer`.
- Kept `/products/refrigeration/pro-series` as a redirect to `/refrigeration/pro-series`.
- Updated the PRO Series links in main navigation, footer, and the PRO comparison card to use `/refrigeration/pro-series`.
- Matched the USA reference content and section order for the first pass:
  - hero video with PRO Series AEM asset
  - intro copy and showroom CTA
  - showroom experience band
  - `The art of freshness` dark media section
  - feature cards for stainless steel interior, air purification, and lighting
  - PRO interior detail/hotspot section
  - inspiration gallery tabs
  - model carousel for 36/48 solid/glass door models
  - expanding kitchen image
  - Explore more Sub-Zero comparison
  - More to discover cards
- Kept typography close to Classic/Designer and checked against the USA reference scale:
  - desktop H1 around `60px`
  - large section headings around `64px`
  - mobile H1 around `45px`
  - no body-level horizontal overflow detected on desktop or mobile checks
- Browser verification:
  - local URL returned `200`
  - page H1 is `PRO Series`
  - title is `PRO Series Refrigeration | Sub-Zero | Sub-Zero Wolf SEA`
  - PRO page has expected video/media sections and 20 interactive buttons
  - mobile viewport `390x844` scroll width stayed below viewport width
- Ran `cmd /c npm run lint` successfully.
- Ran `cmd /c npm run build` successfully.

## Update 2026-07-07 - PRO Series design carousel vertical alignment

Page: `/refrigeration/pro-series` only

Changed:

- Replaced the custom PRO design carousel with the same shared `DesignedWithYou` component used by Classic's `More Classic Series features` section.
- Added a dark theme option to `DesignedWithYou` so PRO can reuse Classic's behavior while keeping the USA PRO dark background treatment.
- Passed PRO-specific title and five PRO Series images into the shared component.
- Kept tabs hidden to match the PRO reference section behavior.
- Added the dark outlined `See more inspiration` CTA under the carousel.
- Kept the next-slide preview crop as `object-left` for the PRO preview image.
- Did not change other PRO sections or other pages.

## Reusable Function Notes - Design Carousel

Use this when the user asks to copy the function/behavior from Classic's `More Classic Series features` section.

Function/component name:

- `DesignedWithYou`

File:

- `src/components/Home/DesignedWithYou.tsx`

Known usage examples:

- Classic page: `src/components/ProductPages/ClassicSeriesPage.tsx`
  - Section title: `More Classic Series features`
  - Uses `DesignedWithYou`
  - Props: `categories={classicSeriesDesignCategories}`, `showTabs={false}`, `ctaLabel={null}`
- PRO page: `src/components/ProductPages/ProSeriesPage.tsx`
  - Section title: `Discover how the PRO Series' striking design fits seamlessly across different aesthetics and styles`
  - Uses the same `DesignedWithYou`
  - Props: `categories={proSeriesDesignCategories}`, `showTabs={false}`, `ctaLabel="See more inspiration"`, `ctaHref="/inspiration"`, `theme="dark"`, `previewImageClassName="object-left"`

Behavior included:

- Active slide state.
- Previous/next arrow controls.
- `1 / total` slide counter.
- Main large image plus next-slide preview image.
- Optional tabs via `showTabs`.
- Optional captions via `captions`.
- Optional CTA via `ctaLabel`.
- Light and dark theme support.

Important rule:

- For future pages that need the same carousel function as Classic's `More Classic Series features`, reuse `DesignedWithYou` and only change `categories`, `title`, `copy`, `theme`, and image/caption data. Do not create a new custom carousel unless the user specifically asks for different behavior.

## Update 2026-07-07 - PRO Series showroom CTA image

Page: `/refrigeration/pro-series` only

Changed:

- Updated the `Start your journey in a showroom` card image from `SZG_Ohio_Showroom_0496_R_hires_CMYK0` to `SZG_Miami_Showroom_51876`.
- This matches the USA reference screenshot with two people in the showroom instead of the darker kitchen/showroom scene.
- Did not change the card layout, copy, or CTA button.

## Update 2026-07-07 - PRO Series model gallery vertical spacing

Page: `/refrigeration/pro-series` only

Changed:

- Adjusted the `For kitchens that mean business` model gallery section to sit lower like the USA reference.
- Increased the section top padding, header grid height, and spacing before the appliance carousel.
- Enlarged the heading from the compact local size to the larger serif scale used in the reference.
- Lowered and slightly enlarged the `See all PRO Series Refrigeration` CTA.
- Browser verification at `1900x950` after spacing pass: heading `y=100`, active model image `y=526`; then lowered the right CTA further to better match the reference.

## Update 2026-07-07 - PRO Series model gallery header wrap and scale

Page: `/refrigeration/pro-series` only

Changed:

- Adjusted the `For kitchens that mean business` header column so desktop wraps like the USA reference: `For kitchens that mean` on the first line and `business` on the second line.
- Reduced the desktop serif heading scale from `4rem` to `3.75rem` to avoid the oversized local look.
- Narrowed the paragraph width so its line breaks sit closer to the USA reference.
- Lowered the `See all PRO Series Refrigeration` CTA slightly with `lg:mt-44`.
- Did not change the model images, carousel behavior, or other sections.

## Update 2026-07-07 - PRO Series model gallery product alignment

Page: `/refrigeration/pro-series` only

Changed:

- Enlarged the active PRO refrigerator render from `lg:h-[560px] lg:w-[430px]` to `lg:h-[620px] lg:w-[475px]`.
- Moved the active refrigerator render upward with `lg:-top-10` to match the USA reference where the product starts higher.
- Increased side-slide spacing from `760px` to `840px` so the offscreen preview products sit closer to the reference crop.
- Moved the model title/copy block up from `lg:top-[200px]` to `lg:top-[180px]`.
- Kept the model tabs and CTA text unchanged.

## Reusable Function Notes - สไลด์ตู้เย็น

Use this when the user asks for the refrigerator model carousel/slider behavior from Classic's `Personalize to find your perfect fit` section.

Function/component name in code:

- `ModelGallery`

User-facing saved name:

- `สไลด์ตู้เย็น`

Reference source:

- Classic page: `src/components/ProductPages/ClassicSeriesPage.tsx`
  - Section heading: `Personalize to find your perfect fit`
  - Component: `ModelGallery`

Current PRO usage:

- PRO page: `src/components/ProductPages/ProSeriesPage.tsx`
  - Section heading: `For kitchens that mean business`
  - Component: `ModelGallery`
  - Data source: local PRO `models` array using four PRO model slides:
    - `36" with Solid Door`
    - `36" with Glass Door`
    - `48" with Solid Door`
    - `48" with Glass Door`

Behavior copied from Classic:

- `active` slide state.
- `visibleModelIndex` state for delayed text swap.
- `descriptionVisible` fade transition.
- `descriptionTimerRef` timeout cleanup.
- `selectModel(index)` handler.
- Wrapped carousel offset so side slides loop correctly.
- Center product image, side preview products, model detail copy, `Explore this model` button, and bottom pill tabs.

Important rule:

- For future refrigerator model sliders, use this `สไลด์ตู้เย็น` pattern from Classic/PRO `ModelGallery`. Do not hand-build a new custom slider unless the user explicitly asks for different behavior.

## Update 2026-07-07 - PRO Series expanding media function restored

Page: `/refrigeration/pro-series` only

Changed:

- Restored the `ExpandingImage` tension zoom function instead of disabling it.
- Recreated the USA-style structure with `section expanding-media-container`, `expanding-media-wrapper`, `expanding-media`, and `tension-zoom tension-zoom--active`.
- Restored dynamic `tension-zoom-border` top/right/bottom/left overlays.
- Restored dynamic image scaling from `1.035` down to `1`.
- Added the `trigger-line` at `55%` and changed scroll progress to use that trigger point.
- This keeps the effect active while allowing the image to appear full-width when the section is aligned like the USA reference.
- Did not change the `สไลด์ตู้เย็น` carousel function.

## Update 2026-07-07 - PRO Series Explore more Sub-Zero alignment

Page: `/refrigeration/pro-series` only

Changed:

- Adjusted the `Explore more Sub-Zero` section to better match the USA reference.
- Reduced the desktop title scale to about `59px` instead of the oversized local look.
- Moved the title/card stack upward with desktop top padding set to `72px`.
- Set the card row gap from the title to `47px` so the cards start at the same visual height as the reference.
- Narrowed the section container to `1340px`, matching the USA-style card starting position on desktop.
- Restored finish swatches in the `FINISH` row:
  - Stainless steel uses a light metallic gradient dot.
  - Panel ready uses a dark dot.
- Did not change the `สไลด์ตู้เย็น` / `ModelGallery` function.

## Completion 2026-07-07 - PRO Series page closed

Page completed for this work session:

- `/refrigeration/pro-series`

Current state:

- User confirmed this page is finished and will continue other pages in a new chat.
- Keep future work scoped to the next requested page.
- Do not keep reworking `/refrigeration/pro-series` unless the user explicitly asks to return to this page.

Final verification already completed:

- `cmd /c npm run lint` passed.
- `cmd /c npm run build` passed.
- Browser verification confirmed the latest `Explore more Sub-Zero` alignment has no horizontal overflow.

## Update 2026-07-07 - Wine Storage clean route first pass

Page: `/refrigeration/wine-storage` only

User direction for this page:

- Use USA reference: `https://www.subzero-wolf.com/refrigeration/wine-storage`
- Local target must be `http://127.0.0.1:3000/refrigeration/wine-storage`
- Do not use `/products/` for this page.
- Keep typography restrained; recurring issue is local fonts becoming larger than USA.
- Future screenshot comparisons: image 1 is USA reference, image 2 is local page to fix.

Changed:

- Added dedicated page component: `src/components/ProductPages/WineStoragePage.tsx`.
- Added clean route: `src/app/refrigeration/wine-storage/page.tsx`.
- Added redirect from old URL `/products/refrigeration/wine-storage` to `/refrigeration/wine-storage`.
- Updated main navigation Wine Storage link to `/refrigeration/wine-storage`.
- Updated footer Wine Storage link to `/refrigeration/wine-storage`.
- Also corrected footer Discover Sub-Zero link to `/refrigeration/discover-sub-zero` to match the existing clean route pattern.
- First-pass page follows the USA Wine Storage order:
  - full-viewport hero video
  - intro with Sub-Zero logo and showroom CTA
  - showroom experience CTA band
  - `Exceptional preservation` / `Endless design possibilities`
  - dark `Crafted for connoisseurs` preservation section
  - large media break
  - detail section
  - wine model gallery for Classic / Designer / Undercounter
  - `Enhance any space in your home` carousel using shared `DesignedWithYou`
  - `Complete your kitchen`
  - `More to discover`
- Used USA AEM assets for all main Wine Storage imagery/video.

Typography and verification:

- Reduced large desktop heading scale after first check to avoid oversized local type.
- Desktop 1900px after adjustment:
  - H1 about `55px`
  - main large section headings about `53-64px`
  - no body horizontal overflow
- Mobile 390px after adjustment:
  - H1 about `42px`
  - no body horizontal overflow
- Browser verification:
  - `/refrigeration/wine-storage` loads with title `Wine Storage Refrigeration | Sub-Zero | Sub-Zero Wolf SEA`
  - old `/products/refrigeration/wine-storage` redirects to `/refrigeration/wine-storage`
  - hero video is present and fills viewport
  - page has 11 main sections, 16 images, and 1 video at local check
- Ran `cmd /c npm run lint` successfully.
- Ran `cmd /c npm run build` successfully.

Current next step:

- Wait for user's screenshot comparison against the USA page and fine-tune spacing/crops section by section.

## Update 2026-07-07 - Wine Storage hero header match

Page: `/refrigeration/wine-storage` only

Changed:

- Matched the attached USA hero screenshot more closely.
- Added optional Header props so changes can be scoped:
  - `showCoveMark`
  - `showAccountLink`
- Enabled those props only in `src/app/refrigeration/wine-storage/page.tsx`.
- Added the `COVE` mark beside Wolf/Sub-Zero in the centered hero header for this page.
- Added the account/user icon between location and search for this page.
- Default `<Header />` usage on other pages remains unchanged.

Verification:

- Browser check confirmed `/refrigeration/wine-storage` header has `Wolf Sub-Zero Cove`.
- Browser check confirmed account link exists at `/account/login`.
- `cmd /c npm run lint` passed.
- `cmd /c npm run build` passed.

## Update 2026-07-07 - Wine Storage hero video source corrected

Page: `/refrigeration/wine-storage` only

Changed:

- Corrected hero video source to match the user's USA HTML snippet exactly:
  - from `/play?assetname=Wine+LP+Hero.mp4#t=0.001`
  - to `/renditions/original/as/Wine LP Hero.mp4#t=0.001`
- Added the hero `data-aem-asset-id` and reference `aria-label` to the hero section.

Verification:

- Browser check confirmed the rendered `<source>` uses `Wine LP Hero.mp4#t=0.001`.
- Browser check confirmed video `currentSrc` resolves to the AEM MP4 rendition URL.
- `cmd /c npm run lint` passed.
- `cmd /c npm run build` passed.

## Update 2026-07-07 - Wine Storage hero video control

Page: `/refrigeration/wine-storage` only

Changed:

- Replaced the simple round play/pause button with a USA-style SVG video control.
- Added circular progress tracking around the button using video `timeupdate` / `loadedmetadata`.
- Control now uses:
  - 60x60 SVG
  - outer muted progress background ring
  - white progress stroke
  - dark translucent center circle
  - pause bars / play triangle state
  - `aria-label` switching between `Pause` and `Play`

Verification:

- Browser DOM check confirmed button size `60x60`.
- Browser DOM check confirmed SVG `viewBox="0 0 60 60"`.
- Browser DOM check confirmed progress circle has `stroke-dasharray`.
- `cmd /c npm run lint` passed.
- `cmd /c npm run build` passed.

## Correction 2026-07-07 - Remove unsolicited Cove header change

Page: `/refrigeration/wine-storage`

Correction:

- User clarified they did not ask to add Cove to the top menu/header.
- Removed the unsolicited `COVE` mark from the centered Header brand marks.
- Removed the unsolicited account/user icon option from the Header.
- Reverted `src/app/refrigeration/wine-storage/page.tsx` back to normal `<Header />`.
- Important rule: do not add Cove/menu/header items unless the user explicitly asks.

Verification:

- `rg` confirmed no `showCoveMark`, `showAccountLink`, `UserRound`, or `COVE` remains in `Header.tsx` / Wine Storage route page.
- `cmd /c npm run lint` passed.
- `cmd /c npm run build` passed.

## Update 2026-07-07 - Wine Storage intro section match

Page: `/refrigeration/wine-storage` only

Changed:

- Updated the intro section after the hero video to match the user's USA screenshot.
- Changed intro background from cream to near-black.
- Changed logo to the white Sub-Zero logo.
- Changed H1 from `Sub-Zero Wine Storage Units` to `Protect your treasured collection`.
- Removed the separate blue uppercase eyebrow treatment.
- Updated body copy and CTA color treatment to white-on-black with a white pill CTA.

Verification:

- Browser DOM check confirmed intro background is `rgb(17, 17, 16)`.
- Browser DOM check confirmed H1 is `Protect your treasured collection`.
- Browser DOM check confirmed CTA background is white and text is dark.
- Browser DOM check confirmed no horizontal overflow at local viewport.
- `cmd /c npm run lint` passed.
- `cmd /c npm run build` passed.

## Update 2026-07-07 - Wine Storage intro H1 reduced

Page: `/refrigeration/wine-storage` only

Changed:

- Reduced intro H1 `Protect your treasured collection` after user feedback that it was too large.
- Changed H1 scale from `clamp(3.2rem,4.55vw,5.35rem)` to `clamp(2.75rem,3.45vw,4rem)`.
- Narrowed max width from `760px` to `700px`.

Verification:

- Browser check at local `1280px` viewport confirmed H1 is about `44px`, down from about `58px`.
- No horizontal overflow detected.
- `cmd /c npm run lint` passed.
- `cmd /c npm run build` passed.

## Correction 2026-07-07 - Wine Storage intro H1 match 80px reference

Page: `/refrigeration/wine-storage` only

Correction:

- User provided devtools screenshot showing USA H1 `Protect your treasured collection` is `80px` and about `684px` wide.
- Adjusted H1 to match that reference size on desktop.
- Changed H1 scale to `clamp(3.6rem,5.56vw,5rem)`.
- Set max width to `684px`.

Verification:

- Browser check at `1440px` viewport confirmed H1 font size is `80px`.
- Browser check confirmed H1 max/rendered width is `684px`.
- No horizontal overflow detected.
- `cmd /c npm run lint` passed.
- `cmd /c npm run build` passed.

## Update 2026-07-07 - Wine Storage intro H1 line arrangement

Page: `/refrigeration/wine-storage` only

Changed:

- Matched the user's screenshot line arrangement for the intro H1.
- Forced H1 line break:
  - `Protect your`
  - `treasured collection`
- Tightened intro vertical spacing:
  - section top padding reduced
  - logo-to-H1 gap reduced slightly
  - H1-to-copy gap reduced slightly
- Kept the desktop H1 scale from the prior reference correction.

Verification:

- Browser DOM check confirmed H1 contains `<br>`.
- Browser DOM check confirmed intro background remains black.
- `cmd /c npm run lint` passed.
- `cmd /c npm run build` passed.

## Update 2026-07-07 - Wine Storage intro H1 prevent third line

Page: `/refrigeration/wine-storage` only

Changed:

- Fixed local H1 breaking into three lines.
- Increased H1 max width from `684px` to `840px`.
- Wrapped `treasured collection` in a `whitespace-nowrap` span so it stays on one line.
- Kept the forced line break after `Protect your`.

Verification:

- Browser DOM check confirmed H1 is `Protect your<br><span>treasured collection</span>`.
- Browser DOM check confirmed `treasured collection` has `white-space: nowrap`.
- Browser DOM check confirmed H1 height equals about two line-heights, not three.
- `cmd /c npm run lint` passed.
- `cmd /c npm run build` passed.

## Update 2026-07-07 - Wine Storage layered image section

Page: `/refrigeration/wine-storage` only

User direction:

- Replace the next section with the same function/pattern as `LayeredImages` after `The pinnacle of food preservation` on `/refrigeration/discover-sub-zero`.
- Use Wine Storage reference data/assets from USA page.
- Do not alter header/menu items.

Changed:

- Removed the two-column `The showroom experience` section from the Wine Storage flow.
- Added `WineLayeredImage`, copied from the `DiscoverSubZeroPage` layered-image function pattern:
  - scroll progress tracking with section ref
  - sticky full-viewport image presentation
  - right-side vertical progress line
  - circular skip-to-next-section button
- Used Wine Storage bar image asset `MED_SLG_LIVINGRM_DEU_3` for the large image.
- Kept the existing sticky `Visit a showroom` CTA behavior.

Verification:

- Browser check confirmed new section `Sub-Zero wine storage in a home bar` exists.
- Browser check confirmed old `The showroom experience` heading is no longer present.
- Browser check confirmed image renders on black background with skip button.
- `cmd /c npm run lint` passed.
- `cmd /c npm run build` passed.

## Update 2026-07-07 - Wine Storage layered image two-slide markup

Page: `/refrigeration/wine-storage` only

Reference provided:

- User attached USA `layered-images-container` markup.
- Required slides:
  - `Exceptional preservation`
  - `Endless design possibilities`
- Required assets:
  - `902166d3-b002-4391-8fea-d2ac16b3bd57`
  - `4cd7a164-2ea7-49be-b27d-0e5ff40a2752`

Changed:

- Updated `WineLayeredImage` from one image to a two-slide layered-images function.
- Added `wineLayeredSlides` data array with USA asset IDs and titles.
- Added scroll timeline, slide translate, image zoom, and title overlay fade behavior copied from Discover Sub-Zero's `LayeredImages` pattern.
- Added two progress hotspot buttons with labels `Image 1 / 2` and `Image 2 / 2`.
- Removed the old duplicate `FeatureSplit` function and render call.

Verification:

- Browser check confirmed layered section has 2 slides.
- Browser check confirmed both USA AEM asset IDs are rendered.
- Browser check confirmed hotspot labels are `Image 1 / 2` and `Image 2 / 2`.
- Browser check confirmed old feature cards are not rendered.
- `cmd /c npm run lint` passed.
- `cmd /c npm run build` passed.

## Update 2026-07-07 - Wine Storage layered title two-line lock

Page: `/refrigeration/wine-storage` only

Changed:

- Forced layered overlay titles to render as exactly two lines.
- Added `titleLines` data for each slide:
  - `Exceptional` / `preservation`
  - `Endless design` / `possibilities`
- Rendered each line as a block with `whitespace-nowrap` so browser wrapping cannot create three lines.

Verification:

- Browser DOM check confirmed both layered headings have exactly two line spans.
- `cmd /c npm run lint` passed.
- `cmd /c npm run build` passed.

## Update 2026-07-07 - Wine Storage layered title font reduced

Page: `/refrigeration/wine-storage` only

Changed:

- Reduced layered overlay title font size after user feedback.
- Changed title scale from `clamp(3rem,4vw,4.75rem)` to `clamp(2.55rem,3.15vw,3.75rem)`.
- Kept titles locked to two lines.

Verification:

- Browser check at `1440px` viewport confirmed layered overlay titles are about `45px`.
- Browser check confirmed both headings still have exactly two lines.
- `cmd /c npm run lint` passed.
- `cmd /c npm run build` passed.

## Update 2026-07-07 - Wine Storage Crafted section header

Page: `/refrigeration/wine-storage` only

Changed:

- Updated the section after layered images to match the user's reference screenshot.
- Removed the blue uppercase eyebrow treatment.
- Changed main H2 from `Advanced preservation technology` to `Crafted for connoisseurs`.
- Moved `Advanced preservation technology...` into the body copy, matching the USA wording.
- Adjusted section background and spacing to continue the black reference treatment.
- Kept the `Explore wine storage` CTA.

Verification:

- Browser check confirmed H2 is `Crafted for connoisseurs`.
- Browser check confirmed no H2 remains with `Advanced preservation technology`.
- Browser check confirmed copy and CTA are present.
- `cmd /c npm run lint` passed.
- `cmd /c npm run build` passed.

## Update 2026-07-07 - Wine Storage reduce Crafted top gap

Page: `/refrigeration/wine-storage` only

Changed:

- Reduced the excessive vertical gap above `Crafted for connoisseurs`.
- Lowered bottom padding on the preceding layered-images section:
  - `pb-24 md:pb-32` to `pb-8 md:pb-12`
- Lowered top padding on the Crafted section:
  - `pt-24 md:pt-32` to `pt-8 md:pt-12`

Verification:

- Browser layout check confirmed no gap between layered section bottom and Crafted section top.
- Browser layout check confirmed Crafted H2 is about `48px` from the section top.
- `cmd /c npm run lint` passed.
- `cmd /c npm run build` passed.

## Update 2026-07-07 - Wine Storage feature row layout

Page: `/refrigeration/wine-storage` only

Changed:

- Adjusted the `Preserve with precision` / `Tailored to your taste` feature row layout to better match the reference screenshot.
- Increased Crafted section content width from `1280px` to `1440px`.
- Changed feature row desktop grid to favor a larger image column.
- Increased feature image sizing and updated image `sizes`.
- Adjusted text block spacing and heading scale.
- Changed `Explore products` CTA from a pill button to a text link with bottom border, matching the reference treatment.

Verification:

- Browser check confirmed first feature image renders larger than before.
- Browser check confirmed CTA is no longer a pill (`border-radius: 0px`) and uses text-link styling.
- `cmd /c npm run lint` passed.
- `cmd /c npm run build` passed.
